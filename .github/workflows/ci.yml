# Temporary validation branch only. This file is never promoted to main.
name: CI
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  not-used:
    runs-on: ubuntu-24.04
    steps:
      - run: exit 1

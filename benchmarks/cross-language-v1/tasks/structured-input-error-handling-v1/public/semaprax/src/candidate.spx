module bench.validation.candidate;

@id("bench.validation.validate")
fn validate(kind: i64, version: i64, payload_len: i64) -> i64
{
    if kind != 7 { 1 } else { if version != 1 { 2 } else { if payload_len < 1 { 3 } else { if payload_len > 64 { 3 } else { 0 } } } }
}

# Full-goal completion matrix

Status: living internal audit of the current product contract.

Audience: maintainers, contributors, reviewers, and technical evaluators.

This document is the authoritative status audit for the complete SEMAPRAX
objective. It deliberately separates three questions:

1. What must the mature product do?
2. What bounded evidence exists today?
3. What still has to be proven before a row is complete?

Historical status transitions belong in the [changelog](../CHANGELOG.md).
Protocol details, exact known-answer digests, test counts, and CI run IDs belong
in the linked versioned specifications. Future sequencing belongs in the
[roadmap](ROADMAP.md).

## Status rules

| Status | Meaning |
| --- | --- |
| Implemented | The full completion gate is covered by executable evidence on every required target. |
| Partial | Useful executable evidence exists, but the full completion gate remains open. |
| Missing | No qualifying executable evidence exists for the row. |

Design text, generated placeholders, source compilation alone, a proof-only
model, or a narrower private target does not complete a broader row. Local,
hosted, private, public, and proof-only evidence are distinct.

In the programme audit below, **Authored, unrun** means that implementation and
executable evidence are present but the row's required programme has not been
executed. **Local, partial** records only the selected local gates identified
by the evidence owner, not an entire work package or the current hosted head.
Neither state supports a hosted or public promotion claim.

## Current goal implementation batch

The additive [iterative lifecycle v2](AGENT-ITERATIVE-LIFECYCLE-V2.md) has six
locally passing focused tests for checked Continue/Complete/Suspend/Fail,
per-turn authorization, bounded execution, cancellation, source-Agent binding,
and invocation-bound evidence. The additive [typed effects v3](AGENT-TYPED-EFFECTS-V3.md)
registry has five locally passing checks for multiple scalar operations, exact
deployed contracts, result validation and call/byte ceilings.
[Direct Runtime v2](AGENT-RUNTIME-V2.md) consumes this checked product from retained
source and joins actual typed execution to immutable roots; its focused local
integration exercises three turns over two operations and refusal before dispatch.
The joined durable integration also passes completed replay without repeated
host calls, changed-task rejection and intent/observation lost-ack recovery.
[Per-operation checkpoints v2](AGENT-OPERATION-CHECKPOINT-V2.md) now pass four
local execution checks and eight decoder checks: every retained stage reserves
fuel before execution; every effect persists intent, observation and checked
transition; trusted-store recovery replays under fresh grants and never
automatically dispatches an uncertain intent. [State migration v2](AGENT-STATE-MIGRATION-V2.md)
now passes a checked pure migration and actual old/new ProgramRoot integration,
carrying cumulative usage into fresh-authorized continuation without initialize.
[Durable migration v3](AGENT-STATE-MIGRATION-V3.md) now passes three local
integration cases for persisted handoff, trusted-store recovery, lost
acknowledgements, exact runtime binding and repeated A→B→C chains, including
replayed B suspension without repeated host calls. Cumulative stages and fuel
include prior handoffs and recovery reservations; two closed handoff codec
checks also pass. Transactional preparation, cross-store coordination,
automatic reconciliation and hosted promotion remain open. [Execution-root association](EXECUTION-ROOT-ASSOCIATION-V1.md)
now has locally passing V1/V2/V3 and iterative invocation association tests.
[Workspace execution association v1](WORKSPACE-EXECUTION-ASSOCIATION-V1.md) joins
one exact immutable semantic-service generation to those consuming producers.
Four new local tests select and execute V1/V2/V3 bindings, reject reminted
receipts, replay against a fresh service, preserve historical bindings after
exact refresh, refuse stale current execution before host calls, and replay
typed durable completion without redispatch. The twelve-test `execution_revision`
selector includes the existing migration chains. This does not promote a
durable semantic service, wire execution route, hosted support, or a new
cross-workspace migration association.
The additive [Workspace execution migration v1](WORKSPACE-EXECUTION-MIGRATION-V1.md)
now joins both retained generations and their original runtime associations
through pure migration, durable recovery, and subsequent migration. Local
injected-handler cases cover A→B→C with recovered B suspension and nine
cumulative calls, reminted receipt refusal, stale destination refusal before
host/store, and preserved terminal evidence after lost acknowledgement.
Recovery still requires the caller's trusted checkpoint and independently
trusted handoff digest. This adds no durable service or execution wire route.
[Generic compiler collections](GENERIC-COMPILER-COLLECTIONS-V1.md) pass focused
source/HIR/graph, ProgramRoot replay, and interpreter/C11 O0/O2/Core-Wasm
execution with repeated success and contract/capacity failure settlement.
[Authored generic variants](GENERIC-AUTHORED-VARIANTS-V1.md) pass four source/HIR
checks, an independent cleanup oracle check, and an 18-profile runtime corpus
covering all eight scalars, both cases and failure settlement across the same
engines. Both variant and collection private ProgramRoot replay and exact
public-profile rejection pass after the HIR linker admission fix. The generated Proposal
client CI matrix is authored for Linux, macOS and Windows; exact-head hosted
evidence remains unobserved. These additions do not complete the programme rows
or widen the public generic boundary.

[Argument inference v3](GENERIC-ARGUMENT-INFERENCE-V3.md) derives complete
ordered vectors through nested omitted calls and generic callers. Scoped
symbolic forwarding retains caller owner/index identities and exact v34/v35
mapping replay, including unused templates and equal-concrete-type mappings.
Fifteen language checks, six independent inference/precheck checks, five graph
mapping checks, three private ProgramRoot replays and three all-eight-scalar
owned runtime corpora pass locally. Native/Wasm allocation counts and interpreter
steps retain evaluation-once behavior on success and contract failure. Broader
lexical/pattern evidence, constraints and hosted promotion remain open; this
does not complete the functions row.

## Current summary

**Overall product objective: Partial**

The long-term contract below contains **50 requirements: 50 Partial, 0
Implemented, 0 Missing**. Each row has at least one bounded executable slice,
but none satisfies its full gate.

The previous “56 Partial / 0 Missing” dashboard mixed the earlier 49 long-term
requirements with seven later milestone rows; the standard library row was
added when its first executable slice landed. That made the denominator change
as work was added. This matrix now keeps the product contract fixed and tracks
release-specific evidence separately.

The strongest current vertical slices are:

- canonical source, stable-ID HIR, and deterministic semantic graph queries;
- bounded replay-checked single-file and managed-workspace changes;
- scalar and selected Copy/owned-data execution through interpreter, native
  C11/Clang, and Core WebAssembly/Node lanes, including the closed flat
  [Owned Byte Variant Algebra v1](OWNED-BYTE-VARIANT-ALGEBRA-V1.md) slice;
- bounded multi-file Project manifests through the additive Project v8
  `owned-data-api.v1` implementation, with one canonical descriptor driving
  Web/npm and unpublished safe Rust package generation;
- the authored frame-payload corpus across interpreter, native C11 O0/O2,
  Core Wasm/Node, generated npm, and generated Rust consumer lanes, plus the
  read-only Project Agent Transport v5 descriptor/carrier surface;
- private desktop/mobile and host-integration evidence.

The Universal Semantic Transaction row additionally has locally passing
`AddDeclaration` evidence: exact old-module path/source/inventory
authentication, Project-wide stable-identity freshness, closed typed Candidate
construction, deterministic replay, and byte preservation outside one source
insertion. This does not promote the row or replace its remaining gate.

SEG-02 additionally has locally passing focused evidence for explicit association of
compiler-admitted AgentDefinition/AgentGraph/Runtime Profile bundles with one
exact Project revision. The bounded AGENT-03 source tranche now admits the
closed `.spx` Agent declaration, retains HIR-equivalent Agent facts and the
byte-identical v1 compatibility products on `ProjectRevision`, and populates the
existing canonical AgentDefinitions node and ProgramRoot segment by default.
Typed in-memory query and service accessors select that same node without a wire
schema change or authority. The additive candidate-safe ProgramRoot-v3 bridge
now independently replays a host-authenticated successor exact context against
a separately frontend-admitted Project and stages one atomic in-memory exact
service refresh with freshly supplied successor facts rather than implicitly
copying current external facts forward.

The largest remaining gaps are general ownership and lifetime safety, stable
public aggregate/resource/component ABIs, a package and dependency ecosystem,
production application tooling, broad target conformance, and the final 1.0
validation product.

## v0.2 product-exit audit

This audit measures the shipped v0.2.0 objective against the broader product
goal. The annotated tag resolves to
`5f6fb9655fdec92c57ab71615cfd7bfa8cc76051`; all 45 jobs in
[release run 33608662244](https://github.com/wavect/semaprax/actions/runs/33608662244)
passed and the prerelease was published. “Exact-tag hosted” below means only
the gate actually selected by that run. It does not imply an ignored,
unprovisioned, broader-browser, physical-device, registry, or production claim.

| Exit criterion | Evidence | Remaining gate |
| --- | --- | --- |
| Multi-module calculator project | Exact-tag hosted | Keep Project Manifest admission and source closure green on subsequent release candidates. |
| Same verified calculator logic on native and browser lanes | Exact-tag hosted | Preserve the identical success/failure corpus on subsequent release candidates and broaden browser engines only when claimed. |
| Several stable-ID functions callable from TypeScript and Rust | Exact-tag hosted; builder remains unpublished | Publish an intentionally supported Rust entry point. |
| Browser calculator consumes Project exports | Exact-tag Chromium, including the display-renamed fixture | Add multi-engine evidence only when broader browser compatibility is claimed. |
| Project daemon inspect/derive/preview/apply/rebuild loop | Exact-tag hosted | Preserve Transport v4's bounded authority contract on subsequent release candidates. |
| Stable external API survives a display rename | Exact-tag hosted | Preserve the complete renamed Project and consumer proof on subsequent release candidates. |
| Project tests demonstrate native/Wasm equivalence | Exact-tag hosted | Preserve the full entry/test and consumer corpus on subsequent release candidates. |
| Multi-module line-filter product | Exact-tag hosted native and Node/Core-Wasm | Add real-browser or multi-engine evidence before claiming that breadth. |
| Full promotion CI for every v0.2.0 release claim | Exact-tag hosted and published | Repeat the complete blocking gate for every later release tag. |

The v0.2.0 prerelease was successfully produced, but this broader product-exit
audit remains **Partial**: the line-filter still lacks the stated browser
breadth and the Rust builder remains unpublished. The tag run executes the
repository's current nonignored test inventory and explicitly selected release
jobs. It does not by itself complete work-package gates that require separate
provisioning, ignored cases, more browser engines, registry publication, or an
explicit support decision.

Evidence owners: [Project Manifest v1](PROJECT-MANIFEST-V1.md) and its additive
v2–v7 references, [Bounded Language Command I/O](BOUNDED-LANGUAGE-COMMAND-IO-V1.md),
[Bounded Language Network I/O](BOUNDED-LANGUAGE-NETWORK-IO-V1.md),
[Project Agent Workflow](PROJECT-AGENT-WORKFLOW-V1.md),
[Wasm Scalar Exports](WASM-SCALAR-EXPORTS-V1.md), and
[Native Rust Interoperability](NATIVE-RUST-INTEROP-V1.md).

## WP-01–WP-15 implementation and promotion audit

This table tracks the bounded developer-preview programme separately from the
49-requirement product contract. It distinguishes authored evidence from
selected local execution; exact artifact/source labels belong to the linked
evidence owners. No row below changes a long-term status to Implemented.

| Work package | Current source state | Authored evidence | Remaining gate |
| --- | --- | --- | --- |
| WP-01 CI decomposition | Exact-tag hosted | Dedicated product/platform matrices and the blocking release aggregation all passed in the v0.2.0 tag run | Preserve this closed aggregation and no-mask policy for later release candidates. |
| WP-02 deterministic version | Exact-tag released | Every archive embedded and reported tag version `0.2.0` and exact commit `5f6fb965`; unpacked human/JSON smokes passed on all three build hosts | Repeat exact tag/version/commit binding for each release; agreement is not a signature. |
| WP-03 release artifacts | Exact-tag released | Linux x86-64, Apple Silicon macOS, and Windows x86-64 archives passed host-local package/unpack smoke and were published with exact checksums | Add targets only with their own build-host smoke; no cross-host reproducibility is claimed. |
| WP-04 v0.2 tagged artifact/release promotion | Complete for v0.2.0 | The release gate, three artifact jobs, closed `SHA256SUMS` inventory, and publication job passed at one exact tag; see [release evidence](RELEASE-PROCESS.md#v020-hosted-release-evidence) | A later release requires a new exact-tag record; this completion does not promote unrelated product rows. |
| WP-05 `doctor` | Exact-tag hosted regression coverage; deterministic signed-release packaging and the Unix signed-generation store have focused local evidence; the Linux provisioner and role-specific worker policies remain unexecuted on an admitted Linux host; ordinary production profiles remain unavailable | [Explicit bounded offline-profile selection](DOCTOR-PROBE-V1.md), sealed-input/bundle parsers, injected-host/version checks, and lower-level settlement tests ran where selected by CI. [Linux Production Provisioner v1](DOCTOR-PRODUCTION-PROVISIONER-V1.md) owns exact release-capsule admission, private namespaces, detached read-only tmpfs root, cgroup limits, held static images, deterministic closed-environment archive construction, and whole-cgroup settlement. [Signed Install v1](DOCTOR-SIGNED-INSTALL-V1.md) locally proves held-root signed-byte installation, cooperative activation/rollback and authenticated inert-stage recovery without execution authority. | Connect the exact active generation to the provisioner without reopening paths, execute the unpacked signed Linux distribution with real tools and hostile authority/settlement cases, record an explicit support handoff, and complete equivalent tool/input, filesystem/broker, network and descendant closure on macOS/Windows; local archive, structural, parser and unit evidence does not establish production confinement. |
| WP-06 `new` | Exact-tag hosted, partial | Generator, scaffold replay/CLI preservation, Project checks, and platform publication regressions ran in the tagged repository suite | Execute the explicitly provisioned unpacked-archive onboarding cases on each advertised archive before claiming installed-product breadth. |
| WP-07 quickstart | Exact-tag hosted source workflow; archive onboarding open | Documentation/examples and Project product jobs exercised the checked source workflow | Execute the documented installation and PATH sequence against each candidate distribution before claiming that end-user path. |
| WP-08 v8 specification | Specified | [Public Owned Data API v1](PUBLIC-OWNED-DATA-API-V1.md) freezes identifiers, admission, lifetime, compatibility, and twelve completion gates | Keep the specification synchronized with the additive implementation and promotion evidence. |
| WP-09 canonical descriptor | Exact-tag hosted regression coverage | Validated-HIR derivation, canonical bytes/digest, independent replay, stable host naming, hostile descriptor cases, and legacy preservation ran in the tagged suite | Retain exact KAT/replay coverage on later profile changes. |
| WP-10 direct `Bytes` npm/Wasm | Exact-tag hosted regression coverage | Profile-specific carrier, copy-out, tuple admission, intrinsic-brand hostility, private-frame exclusion, Node and settlement cases ran where selected | Complete any explicitly provisioned browser-engine cases required by a future public support claim. |
| WP-11 `Option<Bytes>` / `Result<Bytes, i64>` | Exact-tag hosted, partial | Fixed tags, active-payload handling, TypeScript mapping, cleanup, retained evaluation, generated-facade hostility, and inactive-storage cases ran in the tagged inventory | Close any separately provisioned browser/physical equivalence cases required by the intended support claim. |
| WP-12 safe native/Rust SDK | Exact-tag hosted regression and consumer coverage; unpublished | Root-owned provider/SDK, settlement, hostile handles, O0/O2, physical allocation, sanitizer and locked/offline consumer cases ran across the tagged matrices where selected | Make an explicit registry/support decision and execute any still-ignored or separately provisioned cases before that claim. |
| WP-13 Project v8 activation | Exact-tag hosted, partial | Manifest/profile parsing, v1–v7 preservation, routing, retained evaluation, and Windows full-host npm publication regressions ran at the tag | Keep the profile developer-preview until the owning v8 promotion decision explicitly accepts its remaining browser/publication gates. |
| WP-14 frame-payload product | Exact-tag hosted, partial | Shared interpreter/native/Wasm/npm/Rust corpora, display rename, selected consumers, and sanitizer jobs ran in the release matrix | Establish any broader browser-engine and installed-archive consumer breadth before claiming it. |
| WP-15 v8 promotion | Exact-tag hosted release coverage; formal promotion open | The v0.2.0 blocking Project, Rust, browser, sanitizer, host, and hostile suites passed at one exact tag; [Promotion Receipt v1](PROJECT-V8-PROMOTION-RECEIPT-V1.md) now supplies bounded authority-free exact-head observation replay without asserting that any gate ran | Feed independently owned current-head observations into the closed receipt, record an explicit API/package support decision, and close specification-owned provisioned/ignored gaps before calling the generated packages supported or published. |
| Agent Transport v5 follow-on | Exact-tag hosted regression coverage; unpromoted | The focused read-only descriptor/carrier suite and legacy protocol preservation ran in the tagged Rust inventory | Complete cross-language client validation and make an explicit transport promotion decision. |
| Agent Transport v6 public-API follow-on | Focused local executable and generated-codec evidence; unpublished and unpromoted | One opt-in read-only profile returns authenticated Project v8-v11 descriptors and replayed npm carriers with closed profile discriminants; direct equivalence, subject binding, authority exclusion, zero writes, and generated Python/Rust/provisioned-TypeScript codec conformance run locally with cross-profile hostile pairs | Run complete current-tree and hosted gates, package and execute released clients across supported hosts, and make explicit v9-v11 package and transport promotion decisions. |
| Project v9 flat owned record follow-on | Exact-tag hosted regression coverage plus local C11 and safe C++17 provider-consumer execution; unpublished and unpromoted | Descriptor, retained evaluator, Wasm/npm, native provider, safe Rust settlement, Revision Store, and hostile cases ran where selected by the tagged suite; descriptor-derived C and value-only C++17 headers compile, link and execute with the actual provider at O0/O2 | Add fault-injected C++ fail-stop evidence, close remaining separately provisioned and cross-platform physical-consumer gaps, and make an explicit v9 publication/promotion decision before dependent profiles. |
| Project v10 owned UTF-8 follow-on | Exact-tag hosted regression coverage plus local C11 provider-consumer execution; unpublished and unpromoted; blocked on v9 decision | Descriptor/evaluator, cumulative String accounting, Wasm/npm, native owner/provider, safe Rust, UTF-8 hostility and preservation cases ran where selected; the shared descriptor-derived C header links to the actual provider and preserves embedded NUL/multibyte UTF-8 by exact length at O0/O2 | First promote v9, then close remaining provisioned/ignored, cross-platform and browser gates and make an explicit v10 decision. |
| Project v11 nested owned-record follow-on | Focused local executable evidence including a C11 multi-owner consumer; unpublished and unpromoted; blocked on v9/v10 decisions | Separate canonical descriptor and retained evaluator replay passed; generated Core-Wasm/npm ran under Node at the exact cumulative boundary and recovered after +1 rejection; an offline external Rust consumer compiled and ran the generated native package after the same atomic rejection; the descriptor-derived C header links to the actual provider and settles two leaf owners at O0/O2 | Run the complete current-tree and hosted gates, add the remaining specification-owned fault/physical/browser/cross-platform corpus, then make explicit prerequisite and v11 promotion decisions. |
| Project Revision Store v1 follow-on | Exact-tag hosted nonignored coverage; unpromoted | Unix and Windows-entry authority, identity, bounded replay, selected-profile round trips and publication regressions ran in the tagged matrices | Complete opt-in provisioned-host/physical cases and the remaining hostile programme before support promotion. |

The Project v8 implementation now has exact-tag hosted regression evidence but
remains **unpromoted**. The generated npm and Rust packages remain
developer-preview, non-registry surfaces. WP-15's remaining explicit
publication/support decision and specification-owned provisioned gaps still
block describing the bounded owned-data API as supported or published. The
authority-free promotion receipt makes those observations replayable but is
not itself hosted evidence or a support decision.

## Long-term product contract

The “Evidence owner” column points to the document that defines the strongest
current bounded slice. It is not a claim that the linked slice completes the
row.

The Agent-native semantic-program row also includes the bounded additive
[Proposal-to-Runtime v1 compatibility
bridge](AGENT-PROPOSAL-RUNTIME-V1-COMPATIBILITY-V1.md). It maps one exact
Proposal record or Copy-scalar variant, byte-for-byte including its terminal
LF, into the message of the frozen Runtime v1 final action. Proposal decoding
precedes bounded canonical rendering; compilation and rendering perform no
semantic field/case translation, reach no host or tool, and grant no authority.
The row remains **Partial**: direct provider Proposal input, generated Runtime
tool-action/schema integration, broader Proposal values, hosted and
cross-platform generated-client execution, iterative typed lifecycle execution,
live-provider evidence, public ABI, and Runtime v2 consumption remain absent,
while every Runtime v1 wire and known answer stays frozen. A focused local gate
now executes generated record and variant clients under provisioned TypeScript,
Python, and Rust toolchains and re-admits their bytes through the canonical
decoder. A second focused gate selects one source Agent directly into the
existing Lifecycle v1, binding exact lifecycle bytes and semantic source
revision without adding a new wire or authority.

### Semantic foundation

| Requirement | Status | Evidence owner | Complete when |
| --- | --- | --- | --- |
| Source Agent and generated Proposal-client execution | Partial; one checked `.spx` Agent is selected by stable identity, lowered through frozen AgentDefinition v1, bound to the existing one-pass Lifecycle v1, and replayed against both exact lifecycle bytes and semantic source revision. A separate opt-in local gate strict-compiles and executes generated record/variant clients with TypeScript 5.8.3, Python and offline locked Rust, then admits all emitted documents through the canonical decoder. Focused success, refusal, effect failure, missing-Agent/incompatible-role, stale body, exact integer extrema, UTF-8 boundary, stable case and client rejection evidence passes locally; the named Linux CI step is authored but has no pushed run. | [Language-native Agent lowering v1](LANGUAGE-NATIVE-AGENT-LOWERING-V1.md), [Language-native Agent Object v1](LANGUAGE-NATIVE-AGENT-OBJECT-V1.md) | Record hosted evidence, then add iterative typed lifecycle execution, compiled model/effect roles, broader Proposal shapes, packaging/public ABI, Runtime-v2 consumption, provider transport and cross-platform consumers. |
| Agent-native semantic program | Partial; canonical AgentDefinition v1 assigns stable identities to the six agent types and six harness operations, emits deterministic digest-bound AgentGraph v1, and preserves an admitted Agent Runtime Profile v1 byte-for-byte through the existing kernel. Agent Payment Harness v1 additionally binds the exact definition, graph, and independently admitted Economic Policy digests and executes the Runtime-to-payment handoff with disjoint injected hosts, Agent Proposal Schema v1 additionally resolves the definition's Proposal role to one actual verified stable-ID record or variant declaration in a checked module and derives a closed monomorphic scalar grammar, a display-rename-stable type revision, and a typed decoder from that same HIR, with a locally passed gate covering derivation rejections, the enumerated decoder rejections, exact decimal-string integer transport beyond the JSON-safe range, and one offline scripted-provider run whose evidence independently replays. Language-native `.spx` Agent declarations now lower into the same byte-identical compatibility products; Project construction retains real linked-HIR Agent nodes and exact Proposal plus Observation contract facts, and the canonical AgentDefinitions node, ProgramRoot, typed query and semantic service select those facts without adding authority or changing closed wires. AgentDefinition v2 with AgentDeployment v1 additionally separates source-owned identities, tool contracts, effect and model-capability requirements and budget ceilings from an explicit deployment that selects concrete providers/models, granted capabilities, allowed tools, available target features and lower effective limits; binding takes no host, decides capability, tool, budget, target and model incompatibility from the two closed credential-free documents alone, and the locally passed gate shows exact v1 migration round-trip with the frozen graph and profile known answers, provider substitution that leaves the semantic identity unchanged, source changes that stale existing deployments, and one semantic definition run under two offline scripted deployments. Agent Lifecycle v1 additionally binds the definition's four deterministic operation identities to actual verified `.spx` functions in the same HIR ordinary execution uses, validating each stage's signature, parameter ownership modes, declared effects, role types, derived decision shape and the acyclic stage graph before any host work is reachable, and executes one acyclic pass — `initialize`, `observe`, a scripted offline proposal admitted only through Proposal Schema v1, `authorize`, one explicitly injected read operation, `reduce` — through the retained interpreter seam with no re-verification and no source path. The authorizing transition mints an opaque one-use authorization bound to the exact lifecycle policy digest, state carrier and proposal document; its fields are private to a single module, it derives nothing, it is consumed by move at the effect boundary, and the crate's only mint site is reachable exclusively from the validated authorize stage, so a proposal, an observation or a reduction cannot produce one. Its locally passed gate covers the six terminal conditions (completion, authorize refusal, model failure, effect failure, cancellation and per-stage fuel exhaustion), seven stage-binding rejections, byte-identical evidence replay, the refusal to spend an authorization into a substituted state or proposal before the read operation is reached, and the unchanged frozen AgentDefinition, AgentGraph and Runtime v1 profile digests. Stage values stay inside the retained seam's closed vocabulary, so `string` cannot cross a stage boundary and the Proposal role crosses as its exact ordered scalar projection. Agent Checkpoint v1 additionally makes that one lifecycle durable across its single external boundary: `bind_durable_agent` anchors the module, the bound deployment and a caller-supplied policy epoch, and every opaque checkpoint generation binds nine independently recomputed facts, so definition, deployment, bound-product, state-schema, proposal-grammar, lifecycle, source and task drift and a revoked policy epoch each reject a stale checkpoint by its own reason without re-running a stage or writing a generation. A durable run commits an intent before the boundary and a settled observation after it, so a crash between them leaves delivery uncertain; an uncertain operation is never retried automatically but ends in a terminal unknown state or is reconciled by the host to a settled observation or an abandonment, and a reported read failure is treated as uncertain because it is not evidence of non-occurrence. Neither budget ledger is refunded, no `Authorized`, seal or state carrier reaches checkpoint bytes, and a resume mints a fresh grant through the crate's single mint site before comparing the derived operation identity, so a forged, truncated, reordered or rechained journal can only refuse or withhold an effect, never produce authority. Checkpoint bytes are self-verifying, so a partially written generation fails closed with `SPX-G573` and recovery never adopts it, while atomicity itself stays the caller-owned `CheckpointStore` contract; caller inputs are retained as digests only and the settled observation is redactable. Its locally passed gate covers crash injection at all five named boundaries with their recoveries and total boundary-crossing counts, the eight drift and two caller-input rejections, seven malformed-document rejections, the redaction sentinels, an atomic write-and-rename store against a contract-violating torn one, an internally consistent rechained journal forgery that still mints nothing, and the unchanged frozen AgentDefinition, AgentGraph and Runtime v1 profile digests. Checkpoint authenticity independent of the caller's storage contract, external exactly-once execution and provider billing stay nonclaims, and no CLI verb was added. Typed language effects beyond that single injected read, iterative `AgentStep` continue/suspend/fail execution, durable checkpointing beyond that one registered read operation, and Runtime-v2 consumption of AgentGraph or the bound product remain absent. Same-subject selected local Phase 0 v2 evidence includes managed `ACTIVE` and injected result loss after a real local Git ref update; the bounded `function_signature_review_publish_v1` workflow passed its own exact-subject local evidence gate across generated TypeScript/Python/Rust clients and real local SHA-256 Git publication; later focused work adds closed typed SDK failures, per-call authority/blind-spot transcripts, a generated-client contract digest, one installed zero-authority TypeScript workflow package with a passed raw-v5 Unix gate and an authored/unrun pinned-MCP review/publication gate, one session-scoped cancellable candidate-test task exposed through v5/MCP/editor, and authored/unrun generated-file and external-API declaration attachments plus an exact three-declaration analysis-boundary bundle exposed through v5 schemas, generated clients and MCP. The individual routes advance only their owned blind-spot area to partial; the bundle independently replays deployment, generated-file and external-API declarations and advances exactly those three areas to partial without observation or authority. | [RFC 0001](RFC-0001.md), [Language-Native Agent Object v1](LANGUAGE-NATIVE-AGENT-OBJECT-V1.md), [Language-native Agent lowering v1](LANGUAGE-NATIVE-AGENT-LOWERING-V1.md), [Agent Interaction Contract Facts v1](AGENT-INTERACTION-CONTRACT-FACTS-V1.md), [Agent Payment Harness v1](AGENT-PAYMENT-HARNESS-V1.md), [Agent Context v2](AGENT-CONTEXT-V2.md), [Project Agent Transport v5](PROJECT-AGENT-TRANSPORT-V5.md), [Project Revision Store v1](PROJECT-REVISION-STORE-V1.md), [Semantic Workspace Image v1](SEMANTIC-WORKSPACE-IMAGE-V1.md), [Candidate Protocol v2](IMAGE-CANDIDATE-PROTOCOL-V2.md), [Typed Holes v1](PROJECT-CANDIDATE-HOLES-V1.md), [Blind-Spot Declarations v1](PROJECT-CANDIDATE-BLIND-SPOT-DECLARATIONS-V1.md), [Phase 0 Evidence v2](GRAPH-OPERATIONAL-PHASE0-EXECUTION-EVIDENCE-V2.md), [Product Workflow v1](IMAGE-SUPPORTED-PRODUCT-WORKFLOW-V1.md), [Application Error Data v1](IMAGE-AGENT-APPLICATION-ERROR-DATA-V1.md), [Response Accountability v1](IMAGE-SUPPORTED-WORKFLOW-RESPONSE-ACCOUNTABILITY-V1.md), [Packaged TypeScript Workflow SDK v1](IMAGE-PACKAGED-TYPESCRIPT-WORKFLOW-SDK-V1.md), [Candidate Test Tasks v1](IMAGE-CANDIDATE-TEST-TASKS-V1.md), [Phase 1 Product Workflow Evidence v1](GRAPH-OPERATIONAL-PHASE1-PRODUCT-WORKFLOW-EXECUTION-EVIDENCE-V1.md) | Execute the authored declaration, bundle, protocol, generated-client and MCP regressions, preserve the MCP package and real Extension Host gates, extend the compiled lifecycle past one acyclic pass with iterative agent steps, typed authority and a durable lifecycle beyond its one registered read operation, broaden beyond the named scalar workflow, then finish the [graph-operational programme](GRAPH-OPERATIONAL-PROGRAMME.md), persistent and incremental graph lifecycle, general intentions, target-runtime/provider/conformance evidence, separate raw-source authority, and representative validation. |
| Human-readable program | Partial; Canonical Semantic Workspace Revision v1 adds a locally exercised authority-free Project-derived object whose source projection digest remains distinct from its semantic digest, while canonical `.spx` remains the Git representation and all legacy Project/workspace/Image bytes stay unchanged. ProgramRoot v1's locally exercised SEG-02 foundation adds a small content-addressed manifest and independently digested descriptors over those same nine nodes while preserving the legacy bytes. ProgramRoot v2 and Exact Program Context v1 locally bind a non-empty compiler-admitted AgentDefinitions node, exact existing interface descriptors, pathless generated-artifact reports, and verified Project Lock bytes, and select the same v2 root through canonical context, service generation/snapshot, universal query execution/replay, universal transaction validation/replay, service history, and structural-diff base without changing frozen v1 wires. Every exact selector fails closed; exact transaction history retains the authenticated default base workspace identity while ProgramRoot v2 remains an in-memory association. The locally exercised Contracts and Tests Facts v1 object adds an exact stable-ID-sorted association of retained function/function-template contracts and the declared executable test inventory; ProgramRoot v3 appends only its descriptor after the unchanged v2 segment family. Exact Program Context v2 independently replays that association and routes workspace/v3 selection through typed query, transaction, service, and history results without widening their wires. Its candidate-safe refresh bridge independently replays a host-authenticated successor context against a separately compiler-admitted candidate Project, and the service stages one complete generation/cache/history adoption while preserving legacy wires and old snapshots. This is neither contract proof nor coverage nor test execution. Source-owned `.spx` Agent declarations populate the AgentDefinitions node with exact Proposal and Observation contract facts. Context-v1 refresh, execution, publication, and executable Agent transition bodies remain absent. | [RFC 0001](RFC-0001.md), [Canonical Semantic Workspace Revision v1](CANONICAL-SEMANTIC-WORKSPACE-REVISION-V1.md), [ProgramRoot v1](PROGRAM-ROOT-V1.md), [ProgramRoot v2](PROGRAM-ROOT-V2.md), [ProgramRoot v3](PROGRAM-ROOT-V3.md), [Contracts and Tests Facts v1](CONTRACTS-AND-TESTS-FACTS-V1.md), [Exact Program Context v1](EXACT-PROGRAM-CONTEXT-V1.md), [Exact Program Context v2](EXACT-PROGRAM-CONTEXT-V2.md), [Interface and artifact facts v1](INTERFACE-ARTIFACT-FACTS-V1.md), [ProgramRoot Dependency Lock Association v1](PROGRAM-ROOT-DEPENDENCY-LOCK-ASSOCIATION-V1.md), [Agent Interaction Contract Facts v1](AGENT-INTERACTION-CONTRACT-FACTS-V1.md) | Canonical source round-trips every stable language feature with migrations and reviewable diffs; complete source/meaning coverage across the mature language. |
| Verified source semantics | Partial; Canonical Semantic Workspace Revision v1 locally exercises one immutable nine-node projection of an already admitted Project with separate semantic, source-projection, manifest, and dependency-closure components plus exact fresh replay. The locally exercised Contracts and Tests Facts v1 slice separately derives ordered checked contract-expression facts and the declared executable test inventory from retained HIR, and ProgramRoot v3 binds that exact object by an appended descriptor while leaving the frozen canonical workspace and ProgramRoot v1/v2 identities unchanged; it makes no proof, coverage, or execution claim. Exact Program Context v2 adds fail-first workspace/v3 selection and typed-only v3 retention to the unchanged query, transaction, service, and history operations; exact transaction history retains the authenticated default base workspace. Its candidate-safe bridge independently replays a host-authenticated successor exact context against a separately frontend-admitted Project, and the service stages complete generation/cache/index/receipt/history adoption while preserving old snapshots and frozen wires. Universal Semantic Transaction v1 binds one exact revision and validates authority-free display rename, typed whole-function ReplaceBlock, typed AddContract, and typed AddDeclaration through the existing complete Project-candidate path with exact old-state preconditions and byte preservation outside the selected rewrite. Universal Semantic Transaction Composition v1 adds a locally exercised exact structural diff, single-rename rebase, and explicit ordered merge of distinct-target sibling renames. Persistent Incremental Semantic Workspace Service v1 adds a locally exercised transport-neutral process-resident core with immutable revision-bound snapshots, source-exact incremental refresh for ordinary and context-v2 exact generations, atomic generation/cache/index adoption, non-adopting transaction validation, bounded retained indexes, and bounded immutable history. Persistent Semantic Workspace Service Transport v1 locally exercises a bounded single-client stdio process across universal/index/history queries, validation, and caller-owned ordinary refresh; an additive MCP facade exposes the same authority-free service through seven closed tools. Universal Semantic Query v1 locally exercises seven revision-bound operations covering declarations, symbol, context, impact, shared four-operation eligibility, checked ownership/loan facts, and direct retained-HIR consumers with bounded paging and exact replay. Universal Semantic Workflow CLI v1 locally exercises five one-shot queries plus rename, AddContract, and AddDeclaration previews, returning exact core outputs without commit. Complete semantic coverage, context-v1 exact refresh, exact-refresh transport exposure, shared multiprocess/LSP service, durable service state/history, source commit, comments/trivia-preserving composition, and a universal multi-operation algebra remain absent; all frozen v1 and Project Agent Transport v5 wires remain unchanged. | [Architecture](ARCHITECTURE.md), [Canonical Semantic Workspace Revision v1](CANONICAL-SEMANTIC-WORKSPACE-REVISION-V1.md), [Contracts and Tests Facts v1](CONTRACTS-AND-TESTS-FACTS-V1.md), [ProgramRoot v3](PROGRAM-ROOT-V3.md), [Exact Program Context v2](EXACT-PROGRAM-CONTEXT-V2.md), [Universal Semantic Transaction v1](UNIVERSAL-SEMANTIC-TRANSACTION-V1.md), [Universal Semantic Transaction Composition v1](UNIVERSAL-SEMANTIC-TRANSACTION-COMPOSITION-V1.md), [Universal Semantic Query v1](UNIVERSAL-SEMANTIC-QUERY-V1.md), [Persistent Incremental Semantic Workspace Service v1](PERSISTENT-INCREMENTAL-SEMANTIC-SERVICE-V1.md), [Persistent Semantic Workspace Service Transport v1](PERSISTENT-SEMANTIC-SERVICE-TRANSPORT-V1.md), [Persistent Semantic Workspace Service MCP v1](PERSISTENT-SEMANTIC-SERVICE-MCP-V1.md), [Universal Semantic Workflow CLI v1](UNIVERSAL-SEMANTIC-WORKFLOW-CLI-V1.md) | All admitted language features reach validated HIR only after complete type, effect, contract, and ownership checks; exercise broader shared/durable service transports, cover every mature semantic object family, preserve comments and unrelated trivia, and broaden the transaction algebra without bypassing validation. |
| Cross-backend semantic equivalence | Partial; exact-tag nonignored owned-data/String coverage plus focused local canonical numeric-text parity | [Conformance Trace v1](CONFORMANCE-TRACE-V1.md), [String Operations v1](STRING-OPS-V1.md#canonical-numeric-text-v1-2026-09-05), [Public Owned Data API v1](PUBLIC-OWNED-DATA-API-V1.md), [Public Owned UTF-8 API v1](PUBLIC-OWNED-UTF8-API-V1.md), [Native Inline String Settlement v1](NATIVE-INLINE-STRING-SETTLEMENT-V1.md), [Native String Contents v1](NATIVE-STRING-CONTENTS-V1.md), [Internal String Interpreter v1](INTERPRETER-INTERNAL-STRINGS-V1.md), [Standalone Wasm String Settlement v1](WASM-INTERNAL-STRINGS-V1.md#local-validation-record) | Every supported backend passes the same complete behavior, failure, cleanup, and contract corpus; ordinary Wasm String settlement, full native-correction coverage, and separately provisioned opt-in interpreter/standalone-Wasm gates remain open. |
| Atomic agent changes | Partial; the locally exercised Universal Semantic Transaction v1 kernel binds one exact Canonical Semantic Workspace Revision and validates exact-old-name display rename, exact-old-block typed whole-function replacement, AddContract, or AddDeclaration through ProjectCandidate, then exact-replays deterministic authority-free artifacts. AddContract exact-matches the ordered old contract; AddDeclaration authenticates the complete old module and Project-wide identity freshness; both prove byte preservation outside their insertion. Additive Universal Semantic Transaction v2 validates and exact-replays one authored body ReplaceExpression with revision-scoped identity, exact old slice, complete candidate rebuild, and byte preservation outside the span; its ordinary and ProgramRoot-v2/v3 service routes remain read-only, and the CLI derives the precondition before returning exact result/evidence or Candidate structural diff. Universal Semantic Transaction Composition v1 locally exercises a deterministic structural diff, rebases a rename over compatible unrelated drift, and explicitly orders two distinct-target sibling rename Candidates with exact replay and no writes. Its merge result deliberately is not a multi-operation v1 transaction. This slice does not publish, admit comments/noncanonical bases, replace contracts or implicit nodes, compose/rebase/merge ReplaceExpression, support multiple operations per transaction, or establish the complete universal operation algebra. The separate candidate Git slice authors held executable/cwd launch, exact child ambient state and fail-stop process-group settlement without promoting its unexecuted cross-platform gate. | [Universal Semantic Transaction v1](UNIVERSAL-SEMANTIC-TRANSACTION-V1.md), [Universal Semantic Transaction v2](UNIVERSAL-SEMANTIC-TRANSACTION-V2.md), [Universal Semantic Transaction Composition v1](UNIVERSAL-SEMANTIC-TRANSACTION-COMPOSITION-V1.md), [Universal Semantic Workflow CLI v1](UNIVERSAL-SEMANTIC-WORKFLOW-CLI-V1.md), [Patch Evidence v1](SEMANTIC-PATCH-EVIDENCE-V1.md), [Workspace Change v1](SEMANTIC-WORKSPACE-CHANGE-V1.md), [Candidate Git Publication v1](PROJECT-CANDIDATE-GIT-PUBLICATION-V1.md) | General supported single- and multi-file semantic changes replay and publish atomically with recovery and provenance; preserve comments and exact unrelated trivia, broaden typed operations and preconditions, execute the Git substitution, descriptor, deadline and descendant-settlement matrix, and close same-principal repository-content hostility. |

### Language and safety

| Requirement | Status | Evidence owner | Complete when |
| --- | --- | --- | --- |
| Records and algebraic variants | Partial; GEN-06 additionally admits structural nested generic reconstruction and multiple owners with local all-eight-scalar runtime evidence; see the functions row and its owning specifications. Concrete generic owned-byte records substitute every Copy scalar beside `Bytes`, support immutable owned-field updates with partial-failure settlement, and execute locally on interpreter, native O0/O2, and Core-Wasm with hostile-plan replay. The additive flat expression-composition profile keeps one owner and an identical generic-record result while admitting Copy-field projection, top-level immutable update, a borrow match returning a bound Copy field, and an own match reconstructing the same owner for all eight Copy substitutions; update/reconstruction failure settlement, hostile replay, repeated execution on all three engines, and no added aggregate `memory.copy` against the direct-relay baseline are locally exercised. The legacy flat generic-function admission otherwise remains unchanged; the additive nested-nonflat profile relays one owner through an identical result for any bounded acyclic authored-record template tree with explicit Copy-scalar arguments while retaining frozen legacy Graph-v14 bytes and CleanupPlan-v7 semantics; Graph v34 adds exact internal instance ownership. Its focused source/HIR corpus covers `Box<Pair<Bytes,T>>` and `Pair<Box<Bytes>,T>` for all eight scalars, with representative `bool`/`i64` runtime instances on all three engines. Exact cross-file Project evidence retains internal `Pair<Bytes, bool>` behind a scalar-only public boundary. Separately, ScalarV1 admits the exact reachable internal flat generic-owned-record composition independent of provenance while every callable and public signature stays value-scalar; one exact Subject-v3 dependency provides cross-package evidence through `fn() -> i64`. Bounded nested concrete records, flat authored generic variants with one owned case or the exact two-case `[Bytes, Bytes]` shape, and exact compiler-owned `Result<Bytes, Bytes>` ordinary execution plus same-typed postfix `?` run locally on all three engines | [RFC 0002](RFC-0002-ALGEBRAIC-DATA.md), [Owned Byte Record Algebra](OWNED-BYTE-RECORD-ALGEBRA-V1.md), [Concrete Generic Owned-Byte Records](CONCRETE-GENERIC-OWNED-BYTE-RECORDS-V1.md), [Acyclic Nested Owned-Byte Records](NESTED-OWNED-BYTE-RECORDS-V1.md), [Nested Exact Destructuring](NESTED-OWNED-RECORD-DESTRUCTURING-V1.md), [Nested Immutable Update](NESTED-OWNED-RECORD-UPDATE-V1.md), [Owned Byte Variant Algebra](OWNED-BYTE-VARIANT-ALGEBRA-V1.md) | The pre-nested-relay generic-owned corpus is hosted green in run 34031917437, Ubuntu job 101482963175. The nested-relay and identity-forwarding Linux gate is hosted green in run 34048713967, Ubuntu job 101528399406; the independent GEN-05B/GEN-05C Linux job for implementation commit `c27d06f0cf74749804237a43cc71c248b319cfe0` passed in [run 34058787739, job 101555489228](https://github.com/wavect/semaprax/actions/runs/34058787739/job/101555489228). The bounded private generic variant extension has the local evidence recorded above; broader variants, standalone constructors/consuming projections and public generic ABI remain separately gated; then complete nested record/destructuring/update promotion and verify general owned `?`, generic package signatures, nested/resource aggregates, variants, matching, cleanup, and public generic ABIs. |
| Functions, closures, interfaces, implementations, generics | Partial; private Function Values v1/v2 and scalar snapshot Closures v1 have local source/HIR/Graph, interpreter, native O0/O2 and Core-Wasm evidence. Closures retain Copy-scalar snapshots with Graph v37 and SemanticProgram v5/ProgramRoot replay; captured callbacks execute through ordered generic Vec map/filter/fold with repeated explicit host settlement. Closures v2 additionally constructs callbacks inside generic collection bodies and bounded loops, with exact per-instance private identities, mutable scalar body locals, and source-only symbolic template bodies retained even without executable instances; all eight substitutions, cache/graph/ProgramRoot replay and repeated cross-engine snapshot/failure settlement have focused local evidence. Owning captures, iterator interfaces, public callable signatures and hosted promotion remain unfinished. Explicit argument permutation/repetition/concrete forwarding executes across all engines and binds symbolic mappings through additive Graph v35. Generic `Result<Bytes,E>` admits eight Copy errors plus Bytes, and `Result<T,Bytes>` admits all eight Copy successes, with exact conditional settlement and residual propagation. Structural generic record composition now admits nested reconstruction, changed explicit result types, owning branch/call-argument composition, borrowed Copy observation, nested updates and multiple independently owned parameters. Seven composition and five multi-owner source/HIR/graph checks pass locally; all-eight-scalar interpreter/C O0/O2/Core-Wasm runtime corpora pass with repeated execution, allocation settlement, second-argument failure, contract failure and no added Wasm memory.copy. Graph v34 projects concrete ownership and independently selected cleanup, while v35 adds nonidentity mappings. ProgramRoot and bounded context reference the same semantic facts. Existing scalar Project/package boundaries and public descriptors remain separately closed. | [Function Values v1](FUNCTION-VALUES-V1.md), [Function Values v2](FUNCTION-VALUES-V2.md), [Scalar Snapshot Closures v1](CLOSURES-V1.md), [Generic and Loop Closures v2](CLOSURES-V2.md), [Generic Owned Record Composition v2](GENERIC-OWNED-RECORD-COMPOSITION-V2.md), [Generic Multi-Owner Records v1](GENERIC-MULTI-OWNER-RECORDS-V1.md), [Generic Owned Result v1](GENERIC-OWNED-RESULT-V1.md), [Explicit Generic Forwarding v1](GENERIC-EXPLICIT-FORWARDING-V1.md) | GEN-06 evidence here is local. The earlier GEN-05B/GEN-05C bounded corpus passed its named Linux job for `c27d06f0cf74749804237a43cc71c248b319cfe0` in [run 34058787739, job 101555489228](https://github.com/wavect/semaprax/actions/runs/34058787739/job/101555489228). Private authored generic variants and generic bounded Box/Vec operations now have the focused local evidence recorded above and in [authored variants v1](GENERIC-AUTHORED-VARIANTS-V1.md) and [compiler collections v1](GENERIC-COMPILER-COLLECTIONS-V1.md). Remaining work includes broader Result and nested variant composition, constraints/inference after explicit forms, closures/interfaces, public generic ABI and hosted promotion. |
| `Option` and `Result`; no null or unchecked exceptions | Partial; the exact compiler-owned `Result<Bytes, Bytes>` supports ordinary internal construction, own/borrow matching, parameters, results, calls, and same-typed postfix `?` across interpreter, native O0/O2, and Core-Wasm locally; `?` evaluates once, moves Ok ownership, transfers Err into the residual Result, and preserves shared postcondition/finalizer ordering | [RFC 0002](RFC-0002-ALGEBRAIC-DATA.md), [Owned Byte Variant Algebra](OWNED-BYTE-VARIANT-ALGEBRA-V1.md), [Public Owned Data API v1](PUBLIC-OWNED-DATA-API-V1.md) | Mixed/general/nested and generic-function owned propagation, residual conversion, public ABI, hosted promotion, and general target behavior are verified. |
| Immutable-by-default values and explicit mutation | Partial | [Explicit Mutation v1](EXPLICIT-MUTATION-V1.md), [Field Mutation v1](FIELD-MUTATION-V1.md), [Nested Immutable Update](NESTED-OWNED-RECORD-UPDATE-V1.md) | Execute and promote bounded immutable nested reconstruction, then verify general aggregate, collection, borrowed, and concurrency-aware mutation rules. |
| Unique ownership and move safety | Partial; Owning Iterators v1 is implemented privately for scalar `Iter<T>` / `IterStep<T>` with Prelude v7, CleanupPlan v10, Graph v38, and ProgramRoot replay. Local interpreter and native C11 O0/O2 observations cover all eight scalar types, ordered and empty/exhausted steps, early drop, `Done`/`Yield` reconstruction, contract failure, private return, and forged native cursor rejection. The complete focused runtime corpus now passes on Core Wasm too, including exact scope settlement and private return/postcondition failure. Private generic iterator helpers also pass all eight scalar substitutions with callbacks, reconstruction, scoped-identity hostility, and ProgramRoot replay (`generic_iterator` filters); Local and direct `Done` constructors pass canonical round-trip and all engines; the focused ProgramRoot corpus passes with exact v7 declaration retention and legacy Box/Vec/generic preservation. The authored Linux selector is `Require Owning Iterators v1 identity, replay, and backend settlement`, running the library `iterator` filter, the workspace `iterator` filter, and the owned-data `iterator` filter; none is hosted evidence yet. Additive Vec v2 now admits `Vec<Bytes>` owned push/set, reserve, synchronous length/capacity and recursive clear/drop. Local interpreter/native O0/O2/Core-Wasm probes exercise staged two-owner failures, contract failure, allocation refusal and repeated settlement. Prelude v6 binds exact frozen bytes through graph and canonical workspace/ProgramRoot replay. Borrowed element get and broader owned traversal remain closed. Executable selectors: `owned_bytes_vec` (lib), `owned_vec_bytes` (owned_data), `owned_vec_bytes_workspace` (workspace). Hosted promotion remains pending; flat and bounded nested concrete generic records execute with exact cleanup; flat authored generic variants and exact compiler-owned `Result<Bytes, Bytes>` retain exact conditional domains for both owned branches, including dynamic transfers, branch-specific authentication/finalizers, staged-call and arm failures across all three engines; exact same-typed owned Result `?` adds evaluation-once, Ok move, Err residual transfer, topological ownership-only join replay, shared postconditions, sticky failure, and guarded two-case finalization; independent replay rejects hostile type, liveness, case, order, layout, carrier, join, and finalizer substitutions; real cross-file Project linking retains internal identity without widening public descriptors; local Graph v32/v33 evidence also composes owned-variant cleanup with shared loans; one write-once bounded `Bytes` buffer, filled at any admitted `usize` element index and also fillable from inside one bounded `while` through the same-owner replacement `buffer = bytes_set(buffer, index, value)` while its allocation stays outside the loop, and one internal compiler-owned bounded `Vec<T>` over eight Copy scalars execute locally on the reference interpreter, native O0/O2, and internal Core-Wasm with exact owner transfer, repeated success/failure settlement and no Wasm memory copy or growth; a computed buffer index outside its capacity selects the identical `semaprax.byte-buffer.v1` code 1 failure on all three engines before the owner transfer commits, so nothing is written and one exit finalizer still owns the single destruction; Vec construction/full/reserve/index failures retain exact sticky statuses, reserve deterministically selects `max(old_capacity, len + additional)`, set replaces one initialized Copy element, and clear retains capacity; additive prelude v3 is selected only by the three new operations while frozen v1/v2 and old projections remain byte-identical. The bounded `for item in values { body }` source profile traverses one simple immutable Vec binding by an ascending, once-taken length snapshot and discards each body result; resolver lowering reuses existing len/get/while HIR and therefore adds no identity, schema, prelude, backend operation, standard-library declaration, or public ABI. The alloc-tier `std.collections` package has focused local Project/package evidence for eight authenticated aliases with explicit eight-scalar conformance, generated catalogs, and no public exports; public byte and generic collection ABIs stay closed | [RFC 0003](RFC-0003-CLEANUP-AND-RESOURCE-ABI.md), [Owning Iterators v1](OWNING-ITERATORS-V1.md), [Owned Byte Variant Algebra](OWNED-BYTE-VARIANT-ALGEBRA-V1.md), [Concrete Generic Owned-Byte Records](CONCRETE-GENERIC-OWNED-BYTE-RECORDS-V1.md), [Acyclic Nested Owned-Byte Records](NESTED-OWNED-BYTE-RECORDS-V1.md), [Nested Exact Destructuring](NESTED-OWNED-RECORD-DESTRUCTURING-V1.md), [Nested Immutable Update](NESTED-OWNED-RECORD-UPDATE-V1.md), [Shared Loan Plan v1](SHARED-LOAN-PLAN-V1.md), [Projected Owned-Byte Field Shared Borrow v1](PROJECTED-OWNED-BYTE-FIELD-BORROW-V1.md), [Public Owned Data API v1](PUBLIC-OWNED-DATA-API-V1.md), [Owned Bounded Byte Buffer v1](OWNED-BOUNDED-BYTE-BUFFER-V1.md), [Owned Bounded Vec v1](OWNED-BOUNDED-VEC-V1.md), [Owned Bounded Vec v2](OWNED-BOUNDED-VEC-V2.md), [Owned Bounded Vec For Traversal v1](OWNED-BOUNDED-VEC-FOR-TRAVERSAL-V1.md) | Record exact-head hosted evidence for the named iterator selector. [Owning Iterator Loops v1](OWNING-ITERATOR-LOOPS-V1.md) now has focused local all-eight-scalar interpreter, C11 O0/O2, and Core-Wasm evidence for consuming `for own`, generic callbacks, vector accumulation, empty/exhausted traversal, and body-failure cleanup; Graph v39/CleanupPlan v11 and ProgramRoot replay reject changed source and forged loop projections. [Generic Iterator Operations v1](GENERIC-ITERATOR-OPERATIONS-V1.md) adds private one/two-parameter map/filter/fold with all 64 ordered Copy-scalar pairs passing local interpreter, C11 O0/O2, and Core-Wasm runtime checks. Conditional filter updates use [Owning Iterator Renewal v1](OWNING-ITERATOR-RENEWAL-V1.md), additive CleanupPlan v12 and Graph v40; missing reservations, ordinary-transfer substitutions, and schema downgrades reject. Callback/capacity/allocation failures and failure after output staging settle all owners in repeated local execution. SemanticProgram v5/ProgramRoot replay also retains the ordered instances and rejects source drift locally. The existing `iterator` Linux selector covers this corpus; hosted promotion remains pending. Keep iterator interfaces, lazy adapters, owning payloads, public ABI, and the broader LANG-07 goal separate. Record the required hosted generic-owned Linux gate; promote the bounded nested record, destructuring, update, loan, byte-buffer, Vec and bounded traversal corpora; then verify general owned values and `?`, aliases, control flow, FFI, cleanup, public ABI, and target execution. |
| Owned allocation and extraction | Partial; scalar Owned Bounded Box v1 and its authenticated `std.mem` aliases remain frozen. Additive v2 admits compiler `Box<Bytes>` with owned allocation, consuming extraction, recursive lexical Bytes cleanup, Prelude v5 and explicit versioned Wasm imports. Allocation refusal occurs before ownership commit in both plan construction and independent replay. Local focused source/HIR/graph and interpreter/native C11 O0/O2/Core-Wasm probes cover repeated success, contract failures, allocation refusal after Bytes creation, native allocation/authority balance and failure-result preservation, Wasm handle balance and legacy-host refusal. Canonical graph and workspace prelude bindings select v5, including lexical-only Box signatures; exact frozen contract bytes bind ProgramRoot replay. Borrowed `box_get<Bytes>` rejects with SPX-T285. | [Owned Bounded Box v1](OWNED-BOUNDED-BOX-V1.md), [Owned Bounded Box v2](OWNED-BOUNDED-BOX-V2.md), [RFC 0003](RFC-0003-CLEANUP-AND-RESOURCE-ABI.md) | Executable selectors: `cargo test --locked -p semaprax --lib owned_bytes_box` and `cargo test --locked -p semaprax --test owned_data owned_box_bytes`; named Linux CI step authored, hosted promotion pending. Broader owned payloads and composition, general allocation, public ABI, regions, arenas and shared ownership remain open. |
| Borrowed views and lifetime safety | Partial | [Useful Text Consumer v1](USEFUL-TEXT-CONSUMER-V1.md), [Shared Loan Plan v1](SHARED-LOAN-PLAN-V1.md), [Projected Owned-Byte Field Shared Borrow v1](PROJECTED-OWNED-BYTE-FIELD-BORROW-V1.md), [Acyclic Nested Owned-Byte Records](NESTED-OWNED-BYTE-RECORDS-V1.md), [Nested Exact Destructuring](NESTED-OWNED-RECORD-DESTRUCTURING-V1.md), [Projected Owned-Bytes Synchronous Borrowed Call v1](PROJECTED-OWNED-BYTES-BORROWED-CALL-V1.md) | Execute and promote bounded nested field-path loans and borrowed destructuring, then complete general lifetime inference, mutable and escaping borrows, cross-file use, and public host ABI behavior. |
| Regions and arenas | Partial | [Region Report v1](REGION-REPORT-V1.md) | Region inference and runtime placement are implemented and verified; the report alone is insufficient. |
| Shared immutable ARC and managed zones | Partial | [ARC Zone Model v1](ARC-ZONES-V1.md) | Language, runtime, cycle, escape, and concurrency semantics execute on supported targets. |
| Restricted `unsafe` and raw memory | Partial | [Unsafe Boundaries v1](UNSAFE-BOUNDARIES-V1.md) | Raw memory operations, review policy, capability rules, and target conformance are implemented and verified. |
| Checked, wrapping, and saturating arithmetic | Partial; exact-tag nonignored correction coverage | [RFC 0001](RFC-0001.md), [checked `usize` multiplication correction](PORTABLE-INDEXED-BYTE-DATA-V1.md#checked-multiplication-correction) | Retain the tagged ordinary/aggregate Wasm zero-multiplier and owned-cleanup regressions and complete any separately provisioned gates; all numeric widths and named arithmetic modes still require complete cross-backend semantics and tests. |
| Effects and capabilities | Partial | [Capability Manifest v1](CAPABILITY-MANIFEST-V1.md) | Declared effects and build/runtime capabilities are enforced end to end, including dependencies and hosts. |
| Contracts and progressive verification | Partial | [RFC 0001](RFC-0001.md) | Static discharge, bounded proof, runtime obligations, counterexamples, and repair evidence are integrated. |
| Structured concurrency | Partial; the proof model now has a bounded Rust scoped-thread runtime with borrowed captures, stable-ID starts/reports, cooperative cancellation, panic normalization, mandatory join-before-return, and invocation-owned HTTPS work with exact provider settlement and post-settlement publication | [Scoped Task Model v1](SCOPED-TASKS-V1.md), [Structured Tasks Runtime v1](STRUCTURED-TASKS-RUNTIME-V1.md) | Language syntax, `Sendable`/`Shareable` checking, dependency scheduling, deterministic replay, native/Wasm task lowering, and target task execution are verified. |
| Typed hygienic generation | Partial | [Hygienic Generation v1](HYGIENIC-GEN-V1.md) | General typed synthesis is scoped, hygienic, deterministic, and integrated with multi-file semantics and review. |

### Compiler and output targets

| Requirement | Status | Evidence owner | Complete when |
| --- | --- | --- | --- |
| Fast development lane | Partial; exact-tag nonignored interpreter and prepared Project trace coverage | [Interpreter v1](INTERPRETER-V1.md), [Internal String Interpreter v1](INTERPRETER-INTERNAL-STRINGS-V1.md), [Prepared Project Interpreter and Source Trace v1](PROJECT-PREPARED-INTERPRETER-V1.md), [Public Owned Data API v1](PUBLIC-OWNED-DATA-API-V1.md) | Complete separately provisioned cross-platform String admission/replay/conformance gates; then incremental refresh, debugging, hot reload, and semantic equivalence must meet the development-performance target. |
| Optimizing native lane | Partial | [Architecture](ARCHITECTURE.md) | The production native backend covers the mature language, optimization, debug mapping, and supported hosts. |
| WebAssembly core and components | Partial; exact-tag nonignored core/component/String coverage | [Wasm Scalar Exports](WASM-SCALAR-EXPORTS-V1.md), [Wasm Owned ABI](WASM-OWNED-ABI-V1.md), [Public Owned UTF-8 API v1](PUBLIC-OWNED-UTF8-API-V1.md), [Standalone Wasm String Settlement v1](WASM-INTERNAL-STRINGS-V1.md#local-validation-record) | Complete separately provisioned standalone String gates; stable Components, resources, capabilities, multi-engine conformance, and packaging must still be verified. |
| Embedded and real-time | Partial | [Freestanding Profile v1](FREESTANDING-V1.md) | Hardware profiles, linker control, interrupts/RTOS, timing constraints, and representative targets are verified. |
| SIMD and GPU | Partial | [SIMD Report v1](SIMD-REPORT-V1.md) | Vector/GPU lowering, legality, memory behavior, target selection, and performance evidence are implemented. |

### Ecosystem interoperability

| Requirement | Status | Evidence owner | Complete when |
| --- | --- | --- | --- |
| Interface-first packages and target matrices | Partial; exact-tag nonignored semantic-lock, dependency-selection, capsule, and build-v2 coverage; one extensible `semaprax.manifest.v1` table manifest lowers onto every frozen project profile, while ScalarV1 routes replay and link an exact project-local Subject-v3 closure for every declared target. The provenance-independent ScalarV1 body classifier may retain the flat generic-owned record internally while cross-package and selected public signatures stay value-scalar; the exact dependency fixture exposes only `fn() -> i64`, and the manifest, package/report, public-descriptor and Wasm schemas remain frozen. Generated scalar Rust SDKs carry exact Cargo crate inputs; local executable evidence invokes a non-allowlisted exact crate through a typed Rust adapter and observes its result through a SEMAPRAX export; `semaprax lock` and `semaprax resolve` retain their deterministic local gates (`tests/project.rs::package_manifest_v1`, `::project_lock_v1`, `::dependency_resolution_v1`); dependency build and Rust-crate additions remain unpromoted | [Package Manifest v1](PACKAGE-MANIFEST-V1.md), [Project Dependencies v1](PROJECT-DEPENDENCIES-V1.md), [Project Lock v1](PROJECT-LOCK-V1.md), [Project Dependency Resolution v1](PROJECT-DEPENDENCY-RESOLUTION-V1.md), [Package Report v1](PACKAGE-REPORT-V1.md), [Semantic Package Report v2](PACKAGE-REPORT-V2.md), [Offline Semantic Lock v3](OFFLINE-SEMANTIC-PACKAGE-LOCK-V3.md), [Offline Resolver v2](OFFLINE-PACKAGE-RESOLVER-V2.md), [Multi-Package Source Capsule v1](OFFLINE-MULTI-PACKAGE-SOURCE-CAPSULE-V1.md), [Linked Scalar Wasm Package Build v2](OFFLINE-LINKED-SCALAR-WASM-PACKAGE-BUILD-V2.md) | The generic-owned ScalarV1 dependency fixture is hosted on Linux; the independent GEN-05B/GEN-05C job passed in [run 34058787739, job 101555489228](https://github.com/wavect/semaprax/actions/runs/34058787739/job/101555489228). Complete separately provisioned publication/consumer gates; then add generic package signatures, general compatibility negotiation, supported publication, trusted provenance, registry, and conformance. |
| Portable canonical ABI and native fast ABI | Partial | [ABI Report v1](ABI-REPORT-V1.md), [Public Owned Data API v1](PUBLIC-OWNED-DATA-API-V1.md), [Public Owned UTF-8 API v1](PUBLIC-OWNED-UTF8-API-V1.md) | Stable aggregate/resource/borrowed ABIs and cross-language conformance cover supported architectures. |
| C and Objective-C | Partial; local Project-v8-v11 C11 provider/consumer evidence independently compiles, links and executes owned-byte, compiler-owned Option/Result variant, owned-UTF8, flat-record and nested multi-owner integration ABIs at O0/O2, including inactive-case authority rejection, exact-length text and handle settlement | [C Header v1](C-HEADER-V1.md), [Project v8 C/C++ owned-data package v1](PUBLIC-CXX-OWNED-DATA-PACKAGE-V1.md), [Public Flat Owned Record API v1](PUBLIC-FLAT-OWNED-RECORD-API-V1.md), [Public Owned UTF-8 API v1](PUBLIC-OWNED-UTF8-API-V1.md), [Public Nested Owned-Record API v1](PUBLIC-NESTED-OWNED-RECORD-API-V1.md) | Broader import/export, authored-variant/resource ownership, Objective-C adapters, cross-platform consumers, compatibility, maintained distribution, and supported-host conformance are verified. |
| C++ | Partial; local compiled scalar, Project-v8 owned-data, and Project-v9 flat owned-record value-adapter evidence | [C++ Shim v1](CXX-SHIM-V1.md), [C++ scalar package v1](CXX-PACKAGE-V1.md), [Project v8 C++ owned-data package v1](PUBLIC-CXX-OWNED-DATA-PACKAGE-V1.md), [Project v9 flat-record C++ adapter v1](PUBLIC-FLAT-OWNED-RECORD-CXX-ADAPTER-V1.md) | Cross-platform/MSVC compiled consumers, fault-injected aggregate failures, broader nested aggregates/resources and borrowed lifetimes, maintained distribution, compatibility, packaging, and supported-host conformance are verified. |
| Java and Kotlin | Partial | [Android JNI Ownership v1](ANDROID-JNI-OWNERSHIP-V1.md) | Public JVM/JNI artifacts, ownership, exceptions, packaging, and conformance are verified. |
| Swift and Apple frameworks | Partial | [Swift Ownership v1](APPLE-SWIFT-OWNERSHIP-V1.md) | Public Swift/Objective-C API, distributable frameworks, lifecycle, ownership, and device evidence are verified. |
| JavaScript and TypeScript | Partial; exact-tag nonignored Node/TypeScript/Chromium coverage | [Wasm Scalar Exports v1](WASM-SCALAR-EXPORTS-V1.md), [Public Owned Data API v1](PUBLIC-OWNED-DATA-API-V1.md), [Public Owned UTF-8 API v1](PUBLIC-OWNED-UTF8-API-V1.md), [Standalone Wasm String Settlement v1](WASM-INTERNAL-STRINGS-V1.md), [Standalone String Web package](WASM-INTERNAL-STRINGS-WEB-V1.md#local-validation-record) | Complete separately provisioned host and multi-engine browser/runtime breadth; stable general bindings, owned resources, async/callbacks and packaging must still be verified. |
| WIT and WebAssembly Components | Partial | [Public Scalar WIT Interface v1](PUBLIC-SCALAR-WIT-INTERFACE-V1.md), [private WIT Boundary v1](WIT-COMPONENT-BOUNDARY-V1.md) | Extend the retained Project-v1 scalar interface artifact into supported Component publication; source-selected interfaces and resources run through a supported Component Model toolchain on multiple runtimes. |
| OpenAPI, Protobuf/gRPC, GraphQL, and SQL | Partial | [OpenAPI v1](OPENAPI-V1.md) | Import/export, compatibility, live conformance, and all named schema families are verified. |
| Standard library | Partial; thirty-four packages (nine `core`, sixteen `portable` including the pure `std.net`, `std.http`, and `std.async` helpers that compose with Bounded Language Network I/O v1 and six of the seven JSON slice packages, three `alloc`, three `hosted` including the private `std.env` environment snapshot and `std.process` packages, one `agent`, and two `test`) are present for their listed interpreter, native C11, and Core Wasm targets. The additive `std.log` JSON-lines Writer is documented with exactly bundled `std.data.json.utf8`, `std.data.json.write`, and `std.io` dependencies; its Event validates level, UTF-8, and complete Writer capacity before one exact line, with no public exports or ambient effects. Its canonical package has a 59-byte escaped-line golden and five unwritten zero suffix bytes, while fifteen expanded cases retain exact local call closures and required type imports in `tests/project/standard_library/log_cases.spx`. Local gates `logging::log_writer_executes_on_all_three_backends`, `logging::log_writer_preflight_rejects_invalid_events`, and `logging::utf8_ascii_scan_preserves_package_conformance` pass: the canonical package and fifteen expanded cases execute on interpreter/C11 `-O0`/`-O2`/repeated Core Wasm with exact zero-to-three live Bytes bounds, nine rejection cases pass twice with exact contract status, and the existing UTF-8 package conformance remains green. The additive `std.format` Writer slice has seven owned-function-import unit tests, eight individually runnable named SPX tests across interpreter/C11 `-O0`/`-O2`/repeated Core Wasm with a strict two-entry byte arena, five twice-repeated short/forged-output preflight cases with exact `requires`-false status, and passing metadata/catalog regeneration; the pure helper case uses zero allocation and the unchanged per-function static allocation limit requires individual test runs. The `std.collections` alloc package contains only eight authenticated Owned Bounded Vec aliases, explicit eight-Copy-scalar conformance, bundled dependency metadata, generated catalogs, and no public exports. The new `std.mem` alloc package contains only three authenticated Owned Bounded Box aliases, explicit 3-by-8 Copy-scalar conformance, scalar-result example/tests, bundled metadata, and no public exports; its focused local package/catalog and runtime evidence passes. The `std.data.json.dec` alloc package expands every RFC 8259 string escape, including surrogate pairs, into their exact UTF-8 bytes: a pull surface streams them with no allocation, and a buffer surface fills one owned bounded byte buffer of fixed 256-byte capacity through the loop-carried same-owner replacement and compares it, executing on the interpreter, clang C11 `-O0`/`-O2`, and Core Wasm against a one-entry host arena that balances across four re-entries. The additive JSON cursor adapter `decode_into` and the writer adapters `quoted_into`/`count_into` additionally pass the standalone interpreter, native C11 `-O0`/`-O2`, and repeated Core Wasm corpus, including a 300-byte decoded input; six malformed-input, insufficient-capacity, and forged-cursor contract-rejection cases pass. The named `private_json_cursor_roundtrip_executes_across_project_backends` gate additionally passes the local interpreter entry and repeated test, native C11 `-O0`/`-O2`, and repeated Core Wasm with a strict two-entry arena under the unchanged 16 MiB budget. The JSON decoder buffer remains fixed at its allocation site. Separately, [IO Cursors v1](IO-CURSORS-V1.md) adds ordinary Reader/Writer records over caller-supplied Bytes with borrowed observations, consuming transitions, cursor contracts, and internal cross-module transfers. Its Project v8 library route selects no public exports and is available through bundled std.io dependencies in both manifest layouts; local interpreter, C11 O0/O2 and Core-Wasm conformance, contract failures and hostile ownership replay pass. [Typed Path v1](TYPED-PATH-V1.md) now adds the separate `std.path.value` library with NUL-free logical byte prefixes, one-pass lexical observers, root-preserving consuming parents, and caller-buffer joins. Focused local interpreter, C11 O0/O2 and repeated Core-Wasm cases, contract rejection, graph replay hostility and bundled dependency composition pass; existing `std.path` catalog entries remain unchanged. [Filesystem I/O v1](FILESYSTEM-IO-V1.md) adds bounded read/create-new operations, explicit fixture/denied/Unix directory-descriptor providers, Graph v41 replay, and private Project v14 composition through the hosted `std.fs` package. Twenty-nine focused local filesystem tests pass across provider, graph, interpreter, Project, C11 O0/O2 and repeated Core-Wasm cases, including logical prefixes, cumulative limits, invalid-input failure priority and oversized-result cleanup. Catalog and source-structure checks pass separately. [Filesystem I/O v2](FILESYSTEM-IO-V2.md) adds private Project v15 and Graph v42 for typed metadata, canonical immediate directory names, create/remove, and atomic replacement. Focused local typed commands execute on the interpreter, C11 O0/O2 and repeated Core Wasm; provider tests cover directory-descriptor confinement and physical atomic replacement, and hostile directory wires reject before owned-result publication. Streams, recursive traversal, richer metadata, platform path conversion and cross-platform physical-provider promotion remain open. Bundled `std.*` manifest dependencies, cross-file borrowed-text linking, and both hardened built-in templates have focused local evidence. The named Project v16 gate `profile_admission::project_v16_json_cursor_public_facade_replays_and_executes` also passes deterministic npm reconstruction, envelope replay, and Node execution. The private `std.env` environment snapshot package is registered at 32 total packages (16 portable, three hosted) with local carrier, snapshot, provider-constructor, metadata/catalog, module-capability, and link evidence; the named `environment_manifest_is_canonical_and_authority_is_closed` and `environment_package_executes_all_functions_with_injected_snapshot` checks pass locally across the interpreter, native C11, and repeated Core Wasm, with five focused useful-data environment checks also passing. Legacy v1 compatibility remains separately pending. The named `standard_library::process::process_package_executes_all_functions_with_registered_request_shape` gate covers the private `std.process` example, conformance, and bundled-consumer commands on the interpreter, native C11 `-O0`/`-O2`, and Core Wasm, plus five focused physical Darwin provider cases. Linux physical-provider, hosted, public, and broader process gates remain open. The local `testing::test_bytes_package_and_bundled_consumer_execute_across_engines` and `testing::test_bytes_invalid_cursors_and_failure_bits_select_contract_failure` gates pass for the sibling `std.test.bytes` package: source and bundled-consumer commands execute on the interpreter, C11 O0/O2 and repeated Core Wasm, and five invalid-cursor/bit cases retain contract failure with settled Wasm owners. The existing scalar std.test/time consumer also passes. Reusable named byte snapshots now add an owned fixture and Copy comparison result, with exact length/first-difference semantics and cursor preservation; the focused named source and bundled-consumer cases pass across interpreter, C11 O0/O2 and repeated Core Wasm. File-backed snapshots, automatic updates, richer property tests, fuzzing, and hosted promotion remain open. The private `std.agent` package is now an agent-tier package. Its focused package gate `testing::agent_package_and_bundled_consumer_execute_across_engines` and all six linked-role cases under `agent_runtime_v1::execution_revision::typed::linked_agent_*` pass locally; the four testing cases cover interpreter, native C11 `-O0`/`-O2`, and Core Wasm, including `std.agent` epoch `-1`/maximum boundaries and the `std.test.bytes` regression. [Project Linked Agent Migration v1](PROJECT-LINKED-AGENT-MIGRATION-V1.md) adds a separately authenticated migration closure, additive migration root v3, and durable recovery; the focused local imported migration and selection cases pass alongside two workspace linked-binding/currentness cases. Native/Wasm Agent-stage execution, live providers, hosted support, and full `std.agent` completion remain open. | [Standard Library v1](STANDARD-LIBRARY-V1.md), [JSON Cursors v1](JSON-CURSORS-V1.md), [Project Manifest v16](PROJECT-MANIFEST-V16.md), [Project Manifest v18](PROJECT-MANIFEST-V18.md), [Bounded Process I/O v1](BOUNDED-PROCESS-IO-V1.md), [Test Byte Assertions v1](TEST-BYTE-ASSERTIONS-V1.md), [Project Linked Agent Lifecycle v1](PROJECT-LINKED-AGENT-LIFECYCLE-V1.md), [catalog](STANDARD-LIBRARY-CATALOG.md) | Every required module exists at its tier with identities, contracts, effects, examples, conformance on every listed target, and generated documentation; the Everyday profile and remaining offline templates ship. |

### Application platforms

| Requirement | Status | Evidence owner | Complete when |
| --- | --- | --- | --- |
| First-class application/state/UI dialect | Partial | [UI Schema v1](UI-SCHEMA-V1.md) | Typed state/update/view, semantic controls, accessibility, navigation, assets, and platform escape hatches execute. |
| Web | Partial | [Wasm Scalar Exports v1](WASM-SCALAR-EXPORTS-V1.md) | Accessible DOM/CSS, SSR/hydration, packaging, multi-engine execution, and a deployable sample are verified. |
| iOS | Partial | [Swift Ownership v1](APPLE-SWIFT-OWNERSHIP-V1.md) | Public framework/app generation, lifecycle, accessibility, signing metadata, and device/simulator samples are verified. |
| Android | Partial | [Android JNI Ownership v1](ANDROID-JNI-OWNERSHIP-V1.md) | Public AAR/app generation, lifecycle, accessibility, packaging, and emulator/device samples are verified. |
| macOS | Partial | [Desktop App v1](DESKTOP-NATIVE-APP-V1.md) | Public host/UI generation, lifecycle, accessibility, packaging, signing/notarization, and a sample are verified. |
| Windows | Partial | [Desktop UI v1](DESKTOP-NATIVE-UI-V1.md) | Public host/UI generation, lifecycle, accessibility, MSIX/signing metadata, and a sample are verified. |
| Linux | Partial | [Roadmap](ROADMAP.md) | A supported UI/runtime adapter, accessibility, distribution formats, and a representative application are verified. |
| Edge and server | Partial; the explicit Rust host now has authenticated TLS clients and servers, source-level `net_tls_accept`, bind/accept/settlement, deterministic fixture-v2 TLS accept queues, and source-level `https_get` backed by a bounded reusable HTTPS client with HTTP/1.1/2 negotiation, redirects, pooling, and canonical `std.http` bytes; Project v13, `network-run`, Core-Wasm, and the generated npm lane replay fixture v3 under Node and provisioned Chromium, with every browser request confined to loopback; TCP, TLS, and listener operations run under one caller-selected aggregate operation deadline covering resolution, every candidate address, the handshake, partial writes, and readiness waits, with an injected bounded resolver; the generated native C11 adapter carries the same aggregate deadline over name resolution on a POSIX resolver worker it owns and reaps, executed against an injected slow name service and injected `EINTR`, while its `_WIN32` branch is cross-compiled only and Windows execution stays scoped out; Project v13 also emits a libcurl-backed native C11 executable with embedded Mozilla roots, verified TLS 1.2/1.3 policy, HTTP/2 negotiation and HTTP/1.1 fallback; loopback evidence covers untrusted-root and wrong-name TLS rejection, and the six `semaprax.http.v1` outcomes are exercised from checked source | [Bounded Language Network I/O](BOUNDED-LANGUAGE-NETWORK-IO-V1.md), [Bounded Network Services v1](BOUNDED-NETWORK-SERVICES-V1.md), [HTTPS Client Runtime v1](HTTPS-CLIENT-RUNTIME-V1.md), [HTTPS Client I/O v1](HTTPS-CLIENT-IO-V1.md), [Project Manifest v1](PROJECT-MANIFEST-V1.md) | Live browser service adapters, multi-engine browser evidence, HTTP/3, cross-platform libcurl provisioning, DNS policy, structured async services, observability, deployment, and load/conformance tests are verified. |
| Plugins | Partial | [Plugin Manifest v1](PLUGIN-MANIFEST-V1.md) | Capability-limited loading, lifecycle, compatibility, resource limits, packaging, and hostile-plugin tests are verified. |

### Agent economics, review, and operations

| Requirement | Status | Evidence owner | Complete when |
| --- | --- | --- | --- |
| Token-budgeted semantic context | Partial; `semaprax context` accepts either one checked standalone module or one authenticated Project, whose retained typed index supplies bounded cross-file context without a full graph transfer | [Agent Context v2](AGENT-CONTEXT-V2.md), [Economics v1](AGENT-ECONOMICS-V1.md), [Semantic Workspace Image v1](SEMANTIC-WORKSPACE-IMAGE-V1.md) (exact-tag nonignored regression coverage) | Exact model-token budgets, broader semantic edges, persistent indexing, and representative measured savings are verified. |
| Impact analysis before modification | Partial | [Semantic Impact v1](SEMANTIC-IMPACT-V1.md), [Semantic Workspace Image v1](SEMANTIC-WORKSPACE-IMAGE-V1.md) (exact-tag nonignored regression coverage) | Repository-wide call/type/contract/test/schema/target/capability consumers are complete and incremental. |
| Typed holes and compiler-generated repairs | Partial; Installed Fix Plan v1 adds a locally exercised authority-free one-operation catalog and exact current-source plan for the existing `SPX-S103` automatic-function identity repair. It embeds unchanged Diagnostic Repair discovery and supports exact fresh replay, but does not choose the target or persistent ID, instantiate/apply a patch, rank repairs, or cover other diagnostics. | [Diagnostic Repair v1](DIAGNOSTIC-REPAIR-V1.md), [Installed Fix Plan v1](INSTALLED-FIX-PLAN-V1.md) | General obligations and composable sound repairs are generated, ranked, reviewed, and replay-verified. |
| Proof-carrying patches | Partial | [Patch Evidence v2](SEMANTIC-PATCH-EVIDENCE-V2.md) | General semantic claims, tests, targets, capability deltas, provenance, and compatibility are independently verified before commit. |
| Semantic human review | Partial | [Semantic Review v1](SEMANTIC-REVIEW-V1.md) | Complete repository-wide behavioral, API, security, memory, target, migration, and unsafe summaries are evidence-backed. |
| Graph-derived documentation | Partial; `semaprax doc` renders one module's Markdown or `semaprax.doc.v1` documentation from the checked program with the graph revision, the projections harness proves every documented identity of a graph-carried kind is a `graph` node at the same revision on every committed example, and the bundled `help library` and `help shapes` catalogs are rendered from the same model, the former with each signature cross-checked against source; exact library lookups and exact or smallest-exemplar shape lookups come from their generated JSON companions, with guarded single-entry results more than 40 times smaller than their full catalogs in bytes and lexical units; exact stable-topic lookup returns one compiler-checked section of the bundled language card, with every topic more than five times smaller than the full card in both measures; exact diagnostic-code lookup is generated from its correction index, covers every compiler-checked failing example, and makes the guarded answer more than 20 times smaller than the whole index in both measures. Installed Agent Guidance v1 adds a locally passed focused gate for six bounded version-matched embedded projections, but its declaration shapes are explicitly not a complete grammar and its graph document is not a complete stable node/edge catalogue. | [Documentation Projection v1](DOC-PROJECTION-V1.md), [Guided CLI Help v4](CLI-HELP-V4.md), [Installed Agent Guidance v1](INSTALLED-AGENT-GUIDANCE-V1.md) | Document whole projects and their `use` closure, generate the bundled agent skills and standard-library catalog from one complete stable semantic graph document, and expose the projection through the editor and workspace session. |
| Unified 1.0 command surface | Partial; one `semaprax` binary now admits `doc`, `verify` (schema-selected front over every independent evidence verifier and agent graph bundles), `agent inspect`, and the `package report|lock|resolve` namespace, with executable receipts proven byte-identical to the owning routes. The existing `query` declaration search remains unchanged, while Universal Semantic Workflow CLI v1 adds locally exercised Project-only subcommands for declarations, symbol, context, impact and available operations plus read-only `change preview rename-display-name`; these return exact core results/evidence and add no commit or persistent transport. Installed Agent Guidance v1 adds locally exercised exact-core adapters for six `skills get` selectors and static `query --capabilities`, with no live discovery, host grants, or legacy-query replacement. Installed Diagnostics v1 adds a locally exercised exact-core `explain` adapter for one installed static diagnostic identifier; it adds no catalogue CLI, message inference, or repair. Installed Fix Plan v1 adds locally exercised exact-core adapters for the one-operation installed catalog and explicit-source plan; neither applies or ranks repairs. The complementary `help diagnostic` route returns compiler-checked correction-index fixes for exact covered codes or its bounded code inventory. Persistent Semantic Workspace Service Transport v1 adds a locally exercised `service <project>` single-client stdio process with bounded universal/index queries with no MCP/LSP, socket, durable state, or authority grant. The existing `review` verb now also accepts a Project plus exact transaction, returning the core review by default and evidence only on `--evidence`, while legacy patch review remains unchanged. `add` (canonical manifest dependency rows) and `fetch` (cache population from explicit local subjects, no network) are admitted with project-harness gates; `agent run` and `agent replay` execute and re-verify deterministic scripted-transcript runs of the bounded runtime; `agent resume` and `agent reconcile` remain unadmitted and `doctor` now runs in both binaries and only `build --target rust` still needs the private host. | [Unified CLI v1](UNIFIED-CLI-V1.md), [Universal Semantic Workflow CLI v1](UNIVERSAL-SEMANTIC-WORKFLOW-CLI-V1.md), [Installed Agent Guidance v1](INSTALLED-AGENT-GUIDANCE-V1.md), [Installed Diagnostics v1](INSTALLED-DIAGNOSTICS-V1.md), [Installed Fix Plan v1](INSTALLED-FIX-PLAN-V1.md), [Persistent Semantic Workspace Service Transport v1](PERSISTENT-SEMANTIC-SERVICE-TRANSPORT-V1.md), [Documentation Projection v1](DOC-PROJECTION-V1.md), [Guided CLI Help v4](CLI-HELP-V4.md) | Admit the remaining verbs with their own gates, make the private-host split invisible to promoted workflows, and drive the editor surfaces from the same catalog. |
| Editor integration by meaning | Partial; the VS Code adapter has diagnostics on save, whose ranges are the compiler's byte spans translated against the exact saved source into UTF-16, possibly multiline, editor coordinates and whose unclassifiable runs retain the previous diagnostics rather than report a clean project, declaration navigation, callers and code lenses routed through the project that owns an importing module, go-to-declaration by stable identity, callers from the persistent call index, a module documentation view, identity/effect/contract code lenses, an ownership/contract/effect view over the bounded `context` facets, and agent-definition inspection over the compiler's `query --json`, `doc`, `context`, and `agent inspect` routes, safe rename through the replay-checked `patch` route after `impact`, the module graph's cleanup plan per function, and scripted `agent run` trace/evidence views, plus the saved-source session's typed holes, compiler-admitted repairs, and candidate review; exact subject `3fccd30b861d48c9d404eb2698fa2eff510569af` executed the 37-command contributed/registered inventory, the supplementary-character diagnostic range, unclassifiable-run diagnostic retention and verified-run clearing, project-routed declarations/callers/lenses for an importing module, candidate review, and cooperative candidate-test cancellation inside one selected local Visual Studio Code 1.136.1 Extension Host | [VS Code adapter](VSCODE-SAVED-SOURCE-ADAPTER-V1.md), [Unified CLI v1](UNIFIED-CLI-V1.md), [VS Code host evidence v2](GRAPH-OPERATIONAL-VSCODE-HOST-EXECUTION-EVIDENCE-V2.md) | Drive the remaining editor surfaces — typed holes, compiler-admitted repairs, packaging, and manual UI — from the same catalog and prove them in a real Extension Host run; the executed run is one local product on one platform, not hosted or cross-platform evidence. |
| Sandboxed builds and dependencies | Partial; authority-free linked build has exact-tag nonignored coverage, while exact held Project subjects and generated Cargo inputs have local evidence only. ScalarV1 now has Linux-hosted cross-package evidence in the successful bounded GEN-05B/GEN-05C job that exact Subject-v3 replay can exercise the provenance-independent internal flat generic-owned body profile through `fn() -> i64`; this grants no new authority and changes no evidence schema; unpromoted | [Project Dependencies v1](PROJECT-DEPENDENCIES-V1.md), [Capability Manifest v1](CAPABILITY-MANIFEST-V1.md), [Offline Package Lock v1](OFFLINE-PACKAGE-LOCK-V1.md), [Offline Resolver v1](OFFLINE-PACKAGE-RESOLVER-V1.md), [Multi-Package Source Capsule v1](OFFLINE-MULTI-PACKAGE-SOURCE-CAPSULE-V1.md), [Effect-Free Wasm Package Build v1](OFFLINE-PURE-WASM-PACKAGE-BUILD-V1.md), [Linked Scalar Wasm Package Build v2](OFFLINE-LINKED-SCALAR-WASM-PACKAGE-BUILD-V2.md) | Complete separately provisioned publisher evidence, then verify reproducible acquired inputs, generic package signatures and actual least-authority OS sandbox/dependency enforcement. Held source authority and no implicit external tool execution are not a hermetic sandbox. |
| Debugger, profiler, diagnostics, and operations | Partial; Installed Diagnostics v1 adds a bounded compiler-version-matched inventory of static diagnostic code tokens plus explicit unresolved dynamic constructor sites and exact per-code identity/provenance explanation. The five-case focused evidence passes locally; static presence is not runtime reachability, site-specific wording, repair knowledge, backend coverage, or cross-version registry stability. | [Architecture](ARCHITECTURE.md), [Human Diagnostic Locations v1](HUMAN-DIAGNOSTICS-V1.md), [Installed Diagnostics v1](INSTALLED-DIAGNOSTICS-V1.md) | Source-level debugging/profiling, crash and trace mapping, complete runtime diagnostic semantics and repair guidance, observability, and deployment diagnostics cover every backend. |

## Final validation product

Completion requires one maintained offline-first product built from a shared
SEMAPRAX codebase with web, iOS, Android, macOS, Windows, and Linux clients;
native notifications and secure storage; local databases; native or WASI
server execution; authentication; background synchronization; a custom
accelerated visual; one C library; one JavaScript package; and one WebAssembly
component.

Every artifact must be built and exercised in CI or on representative
simulators/devices. Platform-specific implementations must be declared rather
than hidden behind false portability. No current narrow prototype satisfies
this final gate.

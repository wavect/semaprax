schema = "semaprax.manifest.v1"

[package]
name = "catalog-normalizer"
version = "1.0.0"
profile = "owned-data-api.v1"

[modules]
entry = "catalog_normalizer.app"
sources = ["src/app.spx", "src/limits.spx", "src/tests.spx"]
tests = ["catalog_normalizer.tests"]

[exports]
web = ["catalog_normalizer.tests.smoke"]

[dependencies]
std.data.json.dec = "=0.1.0"

# agent_interaction_schema/shape.rs

- MAX_TYPES · constant · L23-L23 — pub(crate) const MAX_TYPES: usize = 64;
- MAX_DEPTH · constant · L26-L26 — pub(crate) const MAX_DEPTH: u32 = 16;
- MAX_STRING_FIELD_BYTES · constant · L28-L28 — pub(crate) const MAX_STRING_FIELD_BYTES: usize = 4_096;
- MAX_BYTES_FIELD_BYTES · constant · L30-L30 — pub(crate) const MAX_BYTES_FIELD_BYTES: usize = 4_096;
- Representation · enum · L36-L44 — pub(crate) enum Representation
- name · function · L47-L57 — pub(crate) fn name(self) -> &'static str
- bounds · function · L60-L68 — pub(crate) fn bounds(self) -> Option<(&'static str, &'static str)>
- max_bytes · function · L72-L78 — pub(crate) fn max_bytes(self) -> Option<usize>
- of_scalar · function · L80-L100 — fn of_scalar(ty: &ResolvedType) -> Option<Self>
- FieldType · enum · L106-L109 — pub(crate) enum FieldType
- FieldRow · struct · L113-L116 — pub(crate) struct FieldRow
- CaseRow · struct · L120-L123 — pub(crate) struct CaseRow
- TypeShape · enum · L127-L130 — pub(crate) enum TypeShape
- TypeDecl · struct · L134-L137 — pub(crate) struct TypeDecl
- TypeGraph · struct · L147-L150 — pub(crate) struct TypeGraph
- get · function · L153-L155 — pub(crate) fn get(&self, stable_id: &str) -> Option<&TypeDecl>
- derive · function · L160-L171 — pub(crate) fn derive(
- derive_one · function · L173-L231 — fn derive_one(
- field_rows · function · L233-L250 — fn field_rows(
- classify · function · L252-L274 — fn classify(
- persistent · function · L276-L285 — fn persistent(resolved: &ResolvedProgram, id: &str, field: &str) -> Result<(), Diagnostic>

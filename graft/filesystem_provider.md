---
covers: []
---
# filesystem_provider.rs

- MAX_PATH_BYTES · constant · L9-L9 — pub const MAX_PATH_BYTES: usize = 4096;
- MAX_FILE_BYTES · constant · L10-L10 — pub const MAX_FILE_BYTES: usize = 65_536;
- MAX_TOTAL_BYTES · constant · L11-L11 — pub const MAX_TOTAL_BYTES: usize = 1_048_576;
- MAX_OPERATIONS · constant · L12-L12 — pub const MAX_OPERATIONS: usize = 64;
- FileFailure · enum · L15-L23 — pub enum FileFailure
- FileKind · enum · L26-L29 — pub enum FileKind
- FileMetadata · struct · L32-L35 — pub struct FileMetadata
- status_code · function · L38-L48 — pub const fn status_code(self) -> u32
- validate_path · function · L53-L64 — pub fn validate_path(path: &[u8]) -> Result<(), FileFailure>
- parent_path · function · L66-L70 — fn parent_path(path: &[u8]) -> Option<&[u8]>
- FileProvider · interface · L76-L95 — pub trait FileProvider
- read · function · L77-L77 — fn read(&mut self, path: &[u8], max: usize) -> Result<Vec<u8>, FileFailure>;
- write_new · function · L78-L78 — fn write_new(&mut self, path: &[u8], data: &[u8]) -> Result<usize, FileFailure>;
- stat · function · L79-L81 — fn stat(&mut self, _path: &[u8]) -> Result<FileMetadata, FileFailure>
- list · function · L82-L84 — fn list(&mut self, _path: &[u8], _max: usize) -> Result<Vec<u8>, FileFailure>
- create_dir · function · L85-L87 — fn create_dir(&mut self, _path: &[u8]) -> Result<usize, FileFailure>
- remove · function · L88-L90 — fn remove(&mut self, _path: &[u8]) -> Result<usize, FileFailure>
- write_atomic · function · L91-L93 — fn write_atomic(&mut self, _path: &[u8], _data: &[u8]) -> Result<usize, FileFailure>
- settle · function · L94-L94 — fn settle(&mut self) {}
- DeniedFileProvider · struct · L98-L98 — pub struct DeniedFileProvider;
- read · function · L101-L103 — fn read(&mut self, _path: &[u8], _max: usize) -> Result<Vec<u8>, FileFailure>
- write_new · function · L104-L106 — fn write_new(&mut self, _path: &[u8], _data: &[u8]) -> Result<usize, FileFailure>
- FixtureFileProvider · struct · L111-L116 — pub struct FixtureFileProvider
- new · function · L119-L149 — pub fn new(
- files · function · L151-L153 — pub fn files(&self) -> &BTreeMap<Vec<u8>, Vec<u8>>
- settlements · function · L154-L156 — pub fn settlements(&self) -> usize
- add_parent_directories · function · L158-L164 — fn add_parent_directories(&mut self, path: &[u8])
- read · function · L168-L178 — fn read(&mut self, path: &[u8], max: usize) -> Result<Vec<u8>, FileFailure>
- write_new · function · L179-L196 — fn write_new(&mut self, path: &[u8], data: &[u8]) -> Result<usize, FileFailure>
- stat · function · L197-L218 — fn stat(&mut self, path: &[u8]) -> Result<FileMetadata, FileFailure>
- list · function · L219-L265 — fn list(&mut self, path: &[u8], max: usize) -> Result<Vec<u8>, FileFailure>
- create_dir · function · L266-L284 — fn create_dir(&mut self, path: &[u8]) -> Result<usize, FileFailure>
- remove · function · L285-L304 — fn remove(&mut self, path: &[u8]) -> Result<usize, FileFailure>
- write_atomic · function · L305-L338 — fn write_atomic(&mut self, path: &[u8], data: &[u8]) -> Result<usize, FileFailure>
- settle · function · L339-L341 — fn settle(&mut self)
- FileAccess · enum · L345-L349 — pub enum FileAccess
- unix · module · L352-L352 — mod unix;
- tests · module · L357-L357 — mod tests;

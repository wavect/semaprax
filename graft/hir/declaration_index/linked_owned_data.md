# hir/declaration_index/linked_owned_data.rs

- extend_linked_owned_data · function · L16-L25 — pub(in crate::hir) fn extend_linked_owned_data(
- extend_linked_scalar_data · function · L27-L36 — pub(in crate::hir) fn extend_linked_scalar_data(
- extend_linked_data · function · L38-L312 — fn extend_linked_data(
- require_fact · function · L47-L73 — fn require_fact<'a>(

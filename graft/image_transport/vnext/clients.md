# image_transport/vnext/clients.rs

- CLIENT_CONTRACT_DOMAIN · constant · L8-L8 — const CLIENT_CONTRACT_DOMAIN: &[u8] = b"semaprax.image-agent-client-contract.v1\0";
- MAX_CLIENT_CONTRACT_BYTES · constant · L9-L9 — const MAX_CLIENT_CONTRACT_BYTES: usize = 32 * 1024 * 1024;
- workflow_client_types · module · L12-L12 — mod workflow_client_types;
- request_types · module · L15-L15 — mod request_types;
- response_types · module · L17-L17 — mod response_types;
- generate · function · L19-L233 — pub(super) fn generate(language: &str, bundle: &Value) -> Result<String>
- validate_workflow_contracts · function · L235-L299 — fn validate_workflow_contracts(workflows: &[Value], methods: &[Value]) -> Result<()>
- client_contract · function · L301-L322 — fn client_contract(
- client_contract_revision · function · L324-L376 — fn client_contract_revision(contract: &Value) -> Result<String>
- FIELDS · constant · L325-L334 — const FIELDS: [&str; 8] = [
- canonical_contract_value · function · L378-L391 — fn canonical_contract_value(value: &Value) -> Value
- ts_union · function · L393-L399 — fn ts_union(values: &[String]) -> String
- py_literal · function · L401-L407 — fn py_literal(values: &[String]) -> String
- rust_enum · function · L409-L417 — fn rust_enum(name: &str, values: &[String]) -> String
- typed_request · function · L422-L436 — fn typed_request(
- request_documents · function · L438-L453 — fn request_documents(bundle: &Value) -> Result<BTreeMap<String, Value>>
- typed_decoder · function · L457-L480 — fn typed_decoder(
- audit_schema · function · L486-L681 — fn audit_schema(schema: &Value, refs: &mut BTreeSet<String>, depth: usize) -> Result<()>
- ResponseNormalizer · struct · L685-L688 — struct ResponseNormalizer
- document · function · L690-L743 — fn document(&mut self, id: &str, source: &Value) -> Result<BTreeMap<String, Value>>
- schema · function · L745-L809 — fn schema(
- raw_rust_literal · function · L818-L824 — fn raw_rust_literal(value: &str) -> String
- response_annotation · function · L826-L860 — fn response_annotation(key: &str) -> bool
- response_documents · function · L862-L937 — fn response_documents(bundle: &Value) -> Result<BTreeMap<String, Value>>
- class_name · function · L938-L951 — fn class_name(value: &str) -> String
- function_name · function · L952-L954 — fn function_name(value: &str) -> String
- ts_type · function · L955-L969 — fn ts_type(schema: &Value) -> String
- py_type · function · L970-L987 — fn py_type(schema: &Value) -> String
- tests · module · L991-L991 — mod tests;

# image_transport/vnext/commit.rs

- Result · type · L17-L17 — type Result<T> = std::result::Result<T, Vec<Diagnostic>>;
- MAX_RECEIPT_BYTES · constant · L18-L18 — const MAX_RECEIPT_BYTES: usize = 1024 * 1024;
- MAX_CHUNK_BYTES · constant · L19-L19 — const MAX_CHUNK_BYTES: usize = 32_768;
- SOURCE_COMMIT_HANDLE_SCHEMA · constant · L20-L20 — pub const SOURCE_COMMIT_HANDLE_SCHEMA: &str = "semaprax.image-source-commit-handle.v1";
- APPROVAL_DOMAIN · constant · L21-L21 — const APPROVAL_DOMAIN: &[u8] = b"semaprax.image-source-commit.host-approval.v1\0";
- RECEIPT_DOMAIN · constant · L22-L22 — const RECEIPT_DOMAIN: &[u8] = b"semaprax.image-source-commit.receipt.v1\0";
- METHODS · constant · L24-L75 — const METHODS: &[Method] = &[
- methods · function · L76-L78 — pub(super) fn methods() -> &'static [Method]
- OwnedAuthority · struct · L80-L80 — struct OwnedAuthority(Box<dyn CandidateGitAuthority>);
- repository · function · L82-L84 — fn repository(&self) -> io::Result<CandidateGitRepository>
- read_ref · function · L85-L87 — fn read_ref(&mut self, reference: &str) -> io::Result<Option<String>>
- read_object · function · L88-L90 — fn read_object(&mut self, oid: &str, max_bytes: usize) -> io::Result<CandidateGitObject>
- write_object · function · L91-L98 — fn write_object(
- compare_and_swap_ref · function · L99-L107 — fn compare_and_swap_ref(
- Approval · struct · L109-L112 — struct Approval
- State · enum · L114-L118 — enum State
- name · function · L120-L126 — fn name(self) -> &'static str
- GitCommitHost · struct · L132-L143 — pub struct GitCommitHost
- new · function · L145-L187 — pub fn new(
- approve · function · L190-L220 — pub fn approve(&mut self, candidate_digest: &str) -> Result<String>
- is_terminal · function · L221-L223 — pub fn is_terminal(&self) -> bool
- manifest · function · L224-L226 — pub fn manifest(&self) -> &Path
- status · function · L228-L230 — pub fn status(&self) -> Value
- execute · function · L234-L310 — pub(super) fn execute(
- report · function · L311-L348 — pub(super) fn report(&self, params: &Map<String, Value>) -> Result<Value>
- digest · function · L350-L364 — fn digest(value: &str) -> Result<()>
- hash · function · L365-L373 — fn hash(domain: &[u8], parts: &[&[u8]]) -> String
- number · function · L374-L382 — fn number(params: &Map<String, Value>, key: &str, default: usize) -> Result<usize>
- failure · function · L383-L385 — fn failure(code: &'static str, message: &'static str) -> Vec<Diagnostic>
- tests · module · L388-L388 — mod tests;

# image_transport/vnext/discovery.rs

- candidate_impact_schemas · module · L13-L13 — mod candidate_impact_schemas;
- clients · module · L15-L15 — mod clients;
- hole_suggestions_schemas · module · L17-L17 — mod hole_suggestions_schemas;
- payload_schemas · module · L19-L19 — mod payload_schemas;
- repair_schemas · module · L21-L21 — mod repair_schemas;
- workflow_metadata · module · L23-L23 — mod workflow_metadata;
- MAX_DISCOVERY_BYTES · constant · L30-L30 — const MAX_DISCOVERY_BYTES: usize = 1000 * 1024;
- Result · type · L35-L35 — type Result<T> = std::result::Result<T, Vec<Diagnostic>>;
- payload · function · L37-L329 — pub(super) fn payload(
- capabilities · function · L331-L368 — fn capabilities(methods: &[&Method], policy: &VNextPolicy, commit: bool) -> Value
- supported_workflows · function · L370-L372 — fn supported_workflows(methods: &[&Method], grants: &[&str], policy: &VNextPolicy) -> Vec<Value>
- descriptor · function · L374-L410 — fn descriptor(method: &Method, policy: &VNextPolicy) -> Value
- method_capability · function · L412-L479 — fn method_capability(method: &Method) -> &'static str
- bundle · function · L481-L697 — fn bundle(descriptors: &[Value], capabilities: &Value) -> Result<Value>
- draft_recovery_schemas · function · L699-L786 — fn draft_recovery_schemas() -> [Value; 2]
- draft_archive_schema · function · L788-L819 — fn draft_archive_schema() -> Value
- references_in · function · L821-L838 — fn references_in(value: &Value, output: &mut BTreeSet<String>)
- bounded · function · L839-L852 — fn bounded(mut value: Value) -> Result<Value>
- invalid · function · L853-L855 — fn invalid(message: &'static str) -> Vec<Diagnostic>
- tests · module · L859-L859 — mod tests;

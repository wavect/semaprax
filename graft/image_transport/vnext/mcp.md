# image_transport/vnext/mcp.rs

- MCP_PROTOCOL_VERSION · constant · L5-L5 — pub const MCP_PROTOCOL_VERSION: &str = "2025-11-25";
- MAX_MCP_REQUEST_BYTES · constant · L6-L6 — pub const MAX_MCP_REQUEST_BYTES: usize = 128 * 1024;
- MAX_MCP_RESPONSE_BYTES · constant · L7-L7 — pub const MAX_MCP_RESPONSE_BYTES: usize = 8 * 1024 * 1024;
- MAX_DEPTH · constant · L8-L8 — const MAX_DEPTH: usize = 128;
- MAX_NODES · constant · L9-L9 — const MAX_NODES: usize = 32_768;
- MAX_ID_BYTES · constant · L10-L10 — const MAX_ID_BYTES: usize = 128;
- Lifecycle · enum · L13-L17 — enum Lifecycle
- McpSession · struct · L22-L27 — pub struct McpSession
- new · function · L30-L47 — pub fn new(inner: VNextSession) -> Result<Self, Vec<Diagnostic>>
- is_terminal · function · L49-L51 — pub fn is_terminal(&self) -> bool
- handle_frame · function · L55-L96 — pub fn handle_frame(&mut self, frame: &[u8]) -> Option<Vec<u8>>
- finish · function · L100-L103 — pub fn finish(&mut self) -> Result<(), Vec<Diagnostic>>
- initialize · function · L105-L132 — fn initialize(&mut self, params: &Map<String, Value>) -> RpcResult
- list · function · L134-L144 — fn list(&self, params: &Map<String, Value>) -> RpcResult
- call · function · L146-L198 — fn call(&mut self, id: &Value, params: &Map<String, Value>) -> Vec<u8>
- oversized_response · function · L200-L203 — fn oversized_response(&mut self) -> Vec<u8>
- RpcResult · type · L206-L206 — type RpcResult = Result<Value, (i64, String)>;
- Request · struct · L207-L211 — struct Request
- decode · function · L213-L251 — fn decode(frame: &[u8]) -> Result<Request, (i64, String)>
- check_tree · function · L253-L272 — fn check_tree(value: &Value, depth: usize, nodes: &mut usize) -> Result<(), (i64, String)>
- parameters · function · L274-L280 — fn parameters(params: &Option<Value>, allowed: &[&str]) -> Result<(), (i64, String)>
- checked_parameters · function · L282-L294 — fn checked_parameters(params: &Map<String, Value>, allowed: &[&str]) -> Result<(), (i64, String)>
- rpc_result · function · L296-L300 — fn rpc_result(id: &Value, result: Value) -> Vec<u8>
- rpc_error · function · L301-L305 — fn rpc_error(id: &Value, code: i64, message: &str) -> Vec<u8>
- escape_json_bytes · function · L307-L323 — fn escape_json_bytes(bytes: &[u8], output: &mut Vec<u8>)
- HEX · constant · L308-L308 — const HEX: &[u8; 16] = b"0123456789abcdef";
- serve_mcp · function · L328-L367 — pub fn serve_mcp<R: BufRead, W: Write>(

# image_transport/vnext/mcp_catalog.rs

- Result · type · L11-L11 — type Result<T> = std::result::Result<T, Vec<Diagnostic>>;
- MAX_TOOLS · constant · L12-L12 — const MAX_TOOLS: usize = 256;
- MAX_CATALOG_BYTES · constant · L13-L13 — const MAX_CATALOG_BYTES: usize = 16 * 1024 * 1024;
- MAX_PAGE_BYTES · constant · L14-L14 — const MAX_PAGE_BYTES: usize = 900 * 1024;
- MAX_PAGE_TOOLS · constant · L15-L15 — const MAX_PAGE_TOOLS: usize = 8;
- MAX_SCHEMA_DEPTH · constant · L16-L16 — const MAX_SCHEMA_DEPTH: usize = 128;
- CURSOR_RESERVE · constant · L17-L17 — const CURSOR_RESERVE: usize = 256;
- Catalog · struct · L19-L24 — pub(super) struct Catalog
- new · function · L28-L30 — pub(super) fn new(policy: &VNextPolicy, commit_enabled: bool) -> Result<Self>
- new_with_package · function · L31-L137 — pub(super) fn new_with_package(
- method · function · L140-L142 — pub(super) fn method(&self, name: &str) -> Option<&'static str>
- page · function · L144-L180 — pub(super) fn page(&self, cursor: Option<&str>) -> Result<Value>
- tool_name · function · L183-L196 — fn tool_name(method: &str) -> Result<String>
- self_contained · function · L201-L240 — fn self_contained(params: &Value, documents: &BTreeMap<String, Value>) -> Result<Value>
- references · function · L242-L260 — fn references(
- rewrite · function · L262-L283 — fn rewrite(
- check_resource · function · L285-L304 — fn check_resource(object: &Map<String, Value>, scope: &str) -> Result<()>
- resolve · function · L306-L320 — fn resolve<'a>(
- walk_schema · function · L322-L382 — fn walk_schema(
- encoded_len · function · L384-L388 — fn encoded_len(value: &Value) -> Result<usize>
- invalid · function · L389-L391 — fn invalid(message: &str) -> Vec<Diagnostic>
- capacity · function · L392-L394 — fn capacity(message: &str) -> Vec<Diagnostic>
- tests · module · L397-L507 — mod tests
- recursive_documents_resolve_locally_without_rewriting_literals · function · L401-L436 — fn recursive_documents_resolve_locally_without_rewriting_literals()
- missing_schema_dependencies_fail_closed · function · L439-L443 — fn missing_schema_dependencies_fail_closed()
- selected_catalogue_pages_are_complete_bound_and_grant_specific · function · L446-L506 — fn selected_catalogue_pages_are_complete_bound_and_grant_specific()

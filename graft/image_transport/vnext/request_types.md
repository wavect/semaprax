# image_transport/vnext/request_types.rs

- rust · module · L8-L8 — mod rust;
- script · module · L10-L10 — mod script;
- MAX_TYPES · constant · L12-L12 — const MAX_TYPES: usize = 4096;
- MAX_WORK · constant · L13-L13 — const MAX_WORK: usize = 65_536;
- MAX_DEPTH · constant · L14-L14 — const MAX_DEPTH: usize = 128;
- MAX_KEYS_BYTES · constant · L15-L15 — const MAX_KEYS_BYTES: usize = 16 * 1024 * 1024;
- MAX_SOURCE_BYTES · constant · L16-L16 — const MAX_SOURCE_BYTES: usize = 900 * 1024;
- GeneratedTypes · struct · L18-L21 — pub(super) struct GeneratedTypes
- Model · struct · L22-L26 — pub(super) struct Model
- Definition · struct · L27-L30 — pub(super) struct Definition
- Field · struct · L31-L35 — pub(super) struct Field
- Shape · enum · L36-L48 — pub(super) enum Shape
- generate · function · L50-L69 — pub(super) fn generate(
- build · function · L71-L136 — fn build(methods: &[Value], documents: &BTreeMap<String, Value>) -> Result<Model>
- Builder · struct · L138-L145 — struct Builder<'a>
- schema · function · L147-L333 — fn schema(
- literal · function · L335-L377 — fn literal(&mut self, value: &Value, scope: &str, depth: usize) -> Result<Shape>
- annotation · function · L380-L382 — fn annotation(key: &str) -> bool
- literal_type · function · L384-L402 — fn literal_type(value: &Value, kind: Option<&Value>) -> Result<()>
- audit · function · L407-L529 — fn audit(object: &Map<String, Value>, scope: &str, resource_root: bool) -> Result<()>
- charge · function · L531-L537 — fn charge(depth: usize, work: &mut usize) -> Result<()>
- productive · function · L542-L574 — fn productive<'a>(
- object_shape · function · L576-L604 — fn object_shape(
- capacity · function · L606-L608 — fn capacity(message: &'static str) -> Vec<crate::diagnostic::Diagnostic>
- tests · module · L612-L612 — mod tests;

# image_transport/vnext/response_types.rs

- rust · module · L7-L7 — mod rust;
- script · module · L9-L9 — mod script;
- MAX_TYPES · constant · L11-L11 — const MAX_TYPES: usize = 4096;
- MAX_WORK · constant · L12-L12 — const MAX_WORK: usize = 65_536;
- MAX_DEPTH · constant · L13-L13 — const MAX_DEPTH: usize = 128;
- MAX_SOURCE_BYTES · constant · L14-L14 — const MAX_SOURCE_BYTES: usize = 900 * 1024;
- GeneratedTypes · struct · L16-L19 — pub(super) struct GeneratedTypes
- Model · struct · L20-L24 — pub(super) struct Model
- Definition · struct · L25-L28 — pub(super) struct Definition
- Field · struct · L29-L33 — pub(super) struct Field
- Shape · enum · L34-L46 — pub(super) enum Shape
- generate · function · L48-L127 — pub(super) fn generate(
- Builder · struct · L129-L138 — struct Builder<'a>
- charge · function · L140-L148 — fn charge(&mut self, depth: usize) -> Result<()>
- schema · function · L150-L345 — fn schema(&mut self, schema: &Value, depth: usize) -> Result<String>
- literal · function · L347-L379 — fn literal(&mut self, value: &Value, depth: usize) -> Result<Shape>
- visit_names_mut · function · L382-L389 — fn visit_names_mut(shape: &mut Shape, mut visit: impl FnMut(&mut String))
- names · function · L391-L398 — fn names(shape: &Shape) -> Vec<&str>
- charge · function · L400-L406 — fn charge(depth: usize, work: &mut usize) -> Result<()>
- productive · function · L408-L440 — fn productive<'a>(
- object_shape · function · L442-L467 — fn object_shape<'a>(
- recursive_names · function · L471-L548 — pub(super) fn recursive_names(model: &Model) -> Result<BTreeSet<String>>
- audit_refinements · function · L550-L623 — fn audit_refinements(fields: &serde_json::Map<String, Value>) -> Result<()>
- capacity · function · L625-L627 — fn capacity(message: &'static str) -> Vec<crate::diagnostic::Diagnostic>
- tests · module · L631-L631 — mod tests;

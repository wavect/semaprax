# image_transport/vnext/test_tasks.rs

- TASK_DOMAIN · constant · L15-L15 — const TASK_DOMAIN: &[u8] = b"semaprax.image-candidate-test-task.v1\0";
- MAX_RESULT_CHUNK_BYTES · constant · L16-L16 — const MAX_RESULT_CHUNK_BYTES: usize = 512 * 1024;
- MAX_ACTIVE_TASKS · constant · L17-L17 — const MAX_ACTIVE_TASKS: usize = 8;
- ACTIVE_TASKS · constant · L18-L18 — static ACTIVE_TASKS: AtomicUsize = AtomicUsize::new(0);
- CANDIDATE · constant · L20-L24 — const CANDIDATE: Parameter = Parameter
- TASK · constant · L25-L29 — const TASK: Parameter = Parameter
- OFFSET · constant · L30-L34 — const OFFSET: Parameter = Parameter
- CHUNK_BYTES · constant · L35-L39 — const CHUNK_BYTES: Parameter = Parameter
- METHODS · constant · L41-L70 — const METHODS: [Method; 4] = [
- methods · function · L72-L74 — pub(super) fn methods() -> impl Iterator<Item = &'static Method>
- Registry · struct · L76-L78 — pub(super) struct Registry
- new · function · L81-L83 — pub(super) const fn new() -> Self
- is_active · function · L85-L87 — pub(super) const fn is_active(&self) -> bool
- request · function · L89-L120 — pub(super) fn request(
- start · function · L122-L173 — fn start(
- task · function · L175-L183 — fn task(&mut self, params: &Map<String, Value>) -> Result<&mut Task, Vec<Diagnostic>>
- invalidate · function · L185-L191 — pub(super) fn invalidate(&mut self)
- drop · function · L195-L197 — fn drop(&mut self)
- Task · struct · L200-L213 — struct Task
- TaskPermit · struct · L216-L218 — struct TaskPermit
- acquire · function · L221-L239 — fn acquire(active: &'static AtomicUsize, limit: usize) -> Result<Self, Vec<Diagnostic>>
- drop · function · L243-L245 — fn drop(&mut self)
- Terminal · enum · L248-L260 — enum Terminal
- release · function · L263-L268 — fn release(&mut self)
- await_terminal · function · L270-L283 — fn await_terminal(&mut self)
- poll · function · L285-L302 — fn poll(&mut self)
- admit · function · L304-L329 — fn admit(&mut self, result: Result<ProjectCandidateTestTaskOutcome, Vec<Diagnostic>>)
- join · function · L331-L340 — fn join(&mut self)
- status · function · L342-L406 — fn status(&self, schema: &str, cancel: bool) -> Value
- result · function · L408-L443 — fn result(&self, offset: usize, max_bytes: usize) -> Result<Value, Vec<Diagnostic>>
- common · function · L446-L454 — fn common(schema: &str, image: &str, project: &str, candidate: &str, task: &str) -> Value
- task_revision · function · L456-L458 — fn task_revision(image: &str, candidate: &str) -> String
- diagnostic_values · function · L460-L465 — fn diagnostic_values(errors: &[Diagnostic]) -> Vec<Value>
- task_error · function · L467-L469 — fn task_error(message: &'static str) -> Vec<Diagnostic>
- worker_error · function · L470-L472 — fn worker_error(message: &'static str) -> Vec<Diagnostic>
- capacity_error · function · L473-L475 — fn capacity_error(message: &'static str) -> Vec<Diagnostic>
- tests · module · L478-L496 — mod tests
- ACTIVE · constant · L481-L481 — static ACTIVE: AtomicUsize = AtomicUsize::new(0);
- task_permit_is_bounded_and_reusable · function · L484-L495 — fn task_permit_is_bounded_and_reusable()

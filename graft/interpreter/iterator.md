# interpreter/iterator.rs

- IteratorValue · struct · L4-L7 — pub(super) struct IteratorValue
- evaluate_iterator_op · function · L9-L125 — pub(super) fn evaluate_iterator_op(
- finish_owned_iterator_next · function · L129-L155 — fn finish_owned_iterator_next(&mut self, iterator: Arc<IteratorValue>) -> Result<Value, Flow>

# interpreter/retained_call.rs

- copy_records · module · L56-L56 — mod copy_records;
- MAX_RETAINED_CALL_PARAMETERS · constant · L78-L78 — pub const MAX_RETAINED_CALL_PARAMETERS: usize = 8;
- RetainedValue · enum · L86-L97 — pub enum RetainedValue
- RetainedRecord · struct · L101-L106 — pub struct RetainedRecord
- RetainedVariant · struct · L110-L114 — pub struct RetainedVariant
- RetainedField · struct · L117-L120 — pub struct RetainedField
- RetainedCallOutcome · enum · L126-L132 — pub enum RetainedCallOutcome
- RetainedCallEvaluation · struct · L136-L145 — pub struct RetainedCallEvaluation
- PreparedRetainedCall · struct · L153-L160 — pub struct PreparedRetainedCall
- RetainedSignature · struct · L163-L166 — struct RetainedSignature
- of · function · L169-L178 — fn of(function: &ResolvedFunction) -> Self
- function_id · function · L184-L186 — pub fn function_id(&self) -> &str
- function_ids · function · L189-L191 — pub fn function_ids(&self) -> impl Iterator<Item = &str>
- parameter_count · function · L195-L197 — pub fn parameter_count(&self) -> usize
- origin_nodes · function · L200-L202 — pub const fn origin_nodes(&self) -> usize
- index_bytes · function · L205-L207 — pub const fn index_bytes(&self) -> usize
- located · function · L210-L216 — fn located(reason: &str, detail: String, span: Span) -> Diagnostic
- prepare_retained_call · function · L226-L305 — pub fn prepare_retained_call(
- evaluate_retained_call · function · L314-L441 — pub fn evaluate_retained_call(
- retained_leaf_is_admitted · function · L444-L453 — fn retained_leaf_is_admitted(ty: &ResolvedType) -> bool
- retained_shape_is_admitted · function · L460-L481 — fn retained_shape_is_admitted(declarations: &hir::DeclarationIndex, ty: &ResolvedType) -> bool
- record_leaves_are_admitted · function · L485-L514 — fn record_leaves_are_admitted(declarations: &hir::DeclarationIndex, root: &ResolvedType) -> bool
- variant_leaves_are_admitted · function · L518-L535 — fn variant_leaves_are_admitted(declarations: &hir::DeclarationIndex, ty: &ResolvedType) -> bool
- ByteStaging · struct · L539-L542 — struct ByteStaging
- allocate · function · L545-L580 — fn allocate(&mut self, bytes: &[u8]) -> Result<Value, String>
- nominal · function · L583-L594 — fn nominal(ty: &ResolvedType) -> Result<(&DeclarationId, &[ResolvedType]), String>
- exactly_once · function · L598-L612 — fn exactly_once<'a>(
- stage · function · L615-L710 — fn stage(
- harvest · function · L713-L741 — fn harvest(
- harvest_record · function · L743-L799 — fn harvest_record(
- harvest_copy · function · L801-L846 — fn harvest_copy(
- harvest_variant · function · L848-L891 — fn harvest_variant(

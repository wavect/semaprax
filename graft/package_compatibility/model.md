# package_compatibility/model.rs

- bf · function · L9-L9 — macro_rules! bf { ($($argument:tt)*) => { bounded_output::budgeted_format(format_args!($($argument)*)) }; }
- Authenticated · struct · L12-L23 — pub(super) struct Authenticated
- Report · struct · L26-L33 — pub(super) struct Report
- Finding · struct · L36-L43 — pub(super) struct Finding
- render · function · L46-L48 — pub(super) fn render(&self) -> String
- render_input · function · L51-L53 — pub(super) fn render_input(value: &Authenticated) -> String

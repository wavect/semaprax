---
covers: []
---
# package_resolver.rs

- catalog · module · L9-L9 — mod catalog;
- model · module · L10-L10 — mod model;
- semver · module · L11-L11 — mod semver;
- solver · module · L12-L12 — mod solver;
- wire · module · L13-L13 — mod wire;
- SCHEMA · constant · L15-L15 — pub const SCHEMA: &str = "semaprax.offline-package-resolution-evidence.v1";
- MAX_REQUIREMENTS · constant · L16-L16 — pub const MAX_REQUIREMENTS: usize = 4;
- MAX_SUBJECTS · constant · L17-L17 — pub const MAX_SUBJECTS: usize = 64;
- MAX_VERSIONS_PER_PACKAGE · constant · L18-L18 — pub const MAX_VERSIONS_PER_PACKAGE: usize = 32;
- MAX_SELECTED_PACKAGES · constant · L19-L19 — pub const MAX_SELECTED_PACKAGES: usize = 4;
- MAX_ALLOWED_CAPABILITIES · constant · L20-L20 — pub const MAX_ALLOWED_CAPABILITIES: usize = 256;
- MAX_SUBJECT_BYTES · constant · L21-L21 — pub const MAX_SUBJECT_BYTES: usize = 17 * 1024 * 1024;
- MAX_TOTAL_SUBJECT_BYTES · constant · L22-L22 — pub const MAX_TOTAL_SUBJECT_BYTES: usize = 128 * 1024 * 1024;
- MAX_EDGES · constant · L23-L23 — pub const MAX_EDGES: usize = 256;
- MAX_DEPTH · constant · L24-L24 — pub const MAX_DEPTH: usize = 32;
- MAX_DECISIONS · constant · L25-L25 — pub const MAX_DECISIONS: usize = 4_096;
- MAX_WORK_UNITS · constant · L26-L26 — pub const MAX_WORK_UNITS: usize = 8 * 1024 * 1024;
- MAX_JSON_DEPTH · constant · L27-L27 — pub const MAX_JSON_DEPTH: usize = 128;
- MAX_RENDER_BYTES · constant · L28-L28 — pub const MAX_RENDER_BYTES: usize = 64 * 1024 * 1024;
- MAX_OUTPUT_BYTES · constant · L29-L29 — pub const MAX_OUTPUT_BYTES: usize = 16 * 1024 * 1024;
- MIN_OUTPUT_BYTES · constant · L30-L30 — const MIN_OUTPUT_BYTES: usize = 4_096;
- DIGEST_DOMAIN · constant · L31-L31 — const DIGEST_DOMAIN: &[u8] = b"semaprax.offline-package-resolution-evidence.v1\0";
- CATALOG_DOMAIN · constant · L32-L32 — const CATALOG_DOMAIN: &[u8] = b"semaprax.offline-package-resolution-catalog.v1\0";
- Requirement · struct · L35-L38 — pub struct Requirement
- ResolutionInput · struct · L41-L46 — pub struct ResolutionInput
- ResolutionOptions · struct · L49-L51 — pub struct ResolutionOptions
- new · function · L54-L61 — pub fn new(max_bytes: usize) -> Result<Self, Diagnostic>
- default · function · L65-L69 — fn default() -> Self
- VerifiedResolution · struct · L73-L76 — pub struct VerifiedResolution
- VerifiedPackageBuildResolution · struct · L78-L81 — pub(crate) struct VerifiedPackageBuildResolution
- VerifiedPackageSourceResolution · struct · L83-L86 — pub(crate) struct VerifiedPackageSourceResolution
- generate · function · L88-L95 — pub fn generate(
- verify · function · L97-L124 — pub fn verify(
- bounded_build · function · L126-L138 — fn bounded_build<'a>(
- BuiltResolution · struct · L140-L143 — struct BuiltResolution<'a>
- build · function · L145-L187 — fn build<'a>(
- verify_for_package_build · function · L189-L229 — pub(crate) fn verify_for_package_build(
- verify_for_package_source · function · L231-L271 — pub(crate) fn verify_for_package_source(
- receipt · function · L273-L290 — fn receipt(evidence: &str) -> Result<VerifiedResolution, Diagnostic>
- validate_options · function · L292-L294 — fn validate_options(options: &ResolutionOptions) -> Result<(), Diagnostic>
- tests · module · L297-L297 — mod tests;

# project/candidate/artifact_delta.rs

- Result · type · L16-L16 — type Result<T> = std::result::Result<T, Vec<Diagnostic>>;
- PROJECT_CANDIDATE_ARTIFACT_DELTA_SCHEMA · constant · L17-L18 — pub const PROJECT_CANDIDATE_ARTIFACT_DELTA_SCHEMA: &str =
- PROJECT_CANDIDATE_ARTIFACT_DELTA_VERIFICATION_SCHEMA · constant · L19-L20 — pub const PROJECT_CANDIDATE_ARTIFACT_DELTA_VERIFICATION_SCHEMA: &str =
- MAX_PROJECT_CANDIDATE_ARTIFACT_DELTA_BYTES · constant · L21-L21 — pub const MAX_PROJECT_CANDIDATE_ARTIFACT_DELTA_BYTES: usize = 8 * 1024 * 1024;
- MAX_FACT_BYTES · constant · L22-L22 — const MAX_FACT_BYTES: usize = 32 * 1024 * 1024;
- MAX_ITEMS · constant · L23-L23 — const MAX_ITEMS: usize = 65_536;
- MAX_VISITS · constant · L24-L24 — const MAX_VISITS: usize = 1_048_576;
- MAX_DEPTH · constant · L25-L25 — const MAX_DEPTH: usize = 128;
- FACT_DOMAIN · constant · L26-L26 — const FACT_DOMAIN: &[u8] = b"semaprax.candidate-artifact-delta.fact.v1\0";
- REPORT_DOMAIN · constant · L27-L27 — const REPORT_DOMAIN: &[u8] = b"semaprax.candidate-artifact-delta.report.v1\0";
- Budget · struct · L30-L33 — struct Budget
- fact · function · L35-L56 — fn fact(&mut self, value: &Value) -> Result<()>
- Count · struct · L36-L36 — struct Count(usize);
- write · function · L38-L44 — fn write(&mut self, bytes: &[u8]) -> io::Result<usize>
- flush · function · L45-L47 — fn flush(&mut self) -> io::Result<()>
- copy · function · L57-L61 — fn copy(&mut self, value: Option<&Value>) -> Result<Value>
- preflight · function · L62-L103 — fn preflight(&mut self, text: &str) -> Result<()>
- Projection · struct · L106-L109 — struct Projection
- artifact_delta · function · L114-L231 — pub fn artifact_delta(
- verify_artifact_delta · function · L233-L257 — pub fn verify_artifact_delta(
- projection · function · L260-L288 — fn projection(
- index · function · L290-L314 — fn index<'a>(
- change · function · L315-L325 — fn change(base: Option<&Value>, candidate: Option<&Value>, equal: bool) -> &'static str
- render · function · L326-L328 — fn render(value: Value) -> Result<String>
- invalid · function · L329-L334 — fn invalid() -> Vec<Diagnostic>
- capacity · function · L335-L340 — fn capacity() -> Vec<Diagnostic>

# project/candidate/draft_suggestions.rs

- PROJECT_HOLE_FILL_SUGGESTIONS_SCHEMA · constant · L10-L10 — pub const PROJECT_HOLE_FILL_SUGGESTIONS_SCHEMA: &str = "semaprax.project-hole-fill-suggestions.v1";
- MAX_PROJECT_HOLE_FILL_SUGGESTIONS_BYTES · constant · L11-L11 — pub const MAX_PROJECT_HOLE_FILL_SUGGESTIONS_BYTES: usize = 64 * 1024;
- MAX_CONTEXT_BYTES · constant · L12-L12 — const MAX_CONTEXT_BYTES: usize = 1024 * 1024;
- MAX_SCOPE · constant · L13-L13 — const MAX_SCOPE: usize = 16_384;
- MAX_CALLS · constant · L14-L14 — const MAX_CALLS: usize = 1024;
- MAX_PARAMETERS · constant · L15-L15 — const MAX_PARAMETERS: usize = 64;
- MAX_ATTEMPTS · constant · L16-L16 — const MAX_ATTEMPTS: usize = 32;
- CONTEXT_DOMAIN · constant · L17-L17 — const CONTEXT_DOMAIN: &[u8] = b"semaprax.project-hole-context.v1\0";
- Result · type · L18-L18 — type Result<T> = std::result::Result<T, Vec<Diagnostic>>;
- hole_fill_suggestions · function · L23-L85 — pub fn hole_fill_suggestions(&self, expected_draft: &str, hole_id: &str) -> Result<String>
- Previews · struct · L89-L93 — struct Previews
- Place · struct · L95-L98 — struct Place<'a>
- Call · struct · L99-L104 — struct Call<'a>
- Inventory · struct · L105-L111 — struct Inventory<'a>
- derive · function · L114-L160 — fn derive(context: &'a Value, body: bool) -> Result<Self>
- enumerate · function · L164-L200 — fn enumerate(&self, visit: &mut impl FnMut(Value) -> bool) -> bool
- advance · function · L203-L212 — fn advance(positions: &mut [usize], options: &[&Vec<&str>]) -> bool
- place_expression · function · L214-L216 — fn place_expression(name: &str) -> Value
- array · function · L218-L228 — fn array<'a>(value: &'a Value, key: &str, maximum: usize) -> Result<&'a [Value]>
- text · function · L230-L235 — fn text<'a>(value: &'a Value, key: &str) -> Result<&'a str>
- bounded_text · function · L237-L245 — fn bounded_text<'a>(value: &'a Value, key: &str, maximum: usize) -> Result<&'a str>
- ownership · function · L247-L255 — fn ownership(value: &Value) -> Result<()>
- grammar · function · L257-L259 — fn grammar(message: &'static str) -> Vec<Diagnostic>
- capacity · function · L260-L262 — fn capacity(message: &'static str) -> Vec<Diagnostic>
- tests · module · L265-L348 — mod tests
- scope · function · L268-L270 — fn scope(name: &str, ty: &str) -> Value
- call · function · L271-L274 — fn call(target: &str, result: &str, parameters: &[&str], permitted: bool) -> Value
- context · function · L275-L277 — fn context(scope: Vec<Value>, calls: Vec<Value>) -> Value
- enumeration_preserves_scope_and_cartesian_order_and_filters_effects_and_self · function · L280-L321 — fn enumeration_preserves_scope_and_cartesian_order_and_filters_effects_and_self()
- lookahead_distinguishes_exact_limit_and_metadata_overflow_is_not_skipped · function · L324-L347 — fn lookahead_distinguishes_exact_limit_and_metadata_overflow_is_not_skipped()

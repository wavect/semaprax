# project/candidate/extraction.rs

- owned · module · L20-L20 — mod owned;
- types · module · L22-L22 — mod types;
- MAX_NODES · constant · L24-L24 — const MAX_NODES: usize = 4096;
- MAX_DEPTH · constant · L25-L25 — const MAX_DEPTH: usize = 256;
- MAX_CAPTURES · constant · L26-L26 — const MAX_CAPTURES: usize = 64;
- Result · type · L27-L27 — type Result<T> = std::result::Result<T, Vec<Diagnostic>>;
- Capture · struct · L29-L34 — struct Capture
- apply · function · L36-L142 — pub(super) fn apply(
- validate · function · L146-L173 — pub(super) fn validate(
- rebase_intent · function · L177-L187 — pub(super) fn rebase_intent(
- expression_selector · function · L189-L193 — fn expression_selector(request: &Value) -> Result<Value>
- capture_plan · function · L195-L404 — fn capture_plan(
- selected_under_conditional · function · L406-L443 — fn selected_under_conditional(
- authenticate_owner_cleanup · function · L445-L546 — fn authenticate_owner_cleanup(
- register_internal · function · L548-L561 — fn register_internal(
- pattern_definitions · function · L563-L596 — fn pattern_definitions(
- record_definitions · function · L598-L618 — fn record_definitions(
- pattern_budget · function · L619-L627 — fn pattern_budget(depth: usize, nodes: &mut usize) -> Result<()>
- validate_request · function · L628-L646 — fn validate_request(value: &Value) -> Result<()>
- text · function · L647-L652 — fn text<'a>(value: &'a Value, key: &str) -> Result<&'a str>
- invalid · function · L653-L655 — fn invalid(message: &'static str) -> Vec<Diagnostic>
- limit · function · L656-L658 — fn limit(message: &'static str) -> Vec<Diagnostic>
- owner_invalid · function · L659-L661 — fn owner_invalid(message: &'static str) -> Vec<Diagnostic>
- owner_auth · function · L662-L664 — fn owner_auth(message: &'static str) -> Vec<Diagnostic>
- owner_replay · function · L665-L667 — fn owner_replay(message: &'static str) -> Vec<Diagnostic>

# project/image_facets/instances.rs

- IMAGE_FUNCTION_INSTANCES_SCHEMA · constant · L5-L5 — pub const IMAGE_FUNCTION_INSTANCES_SCHEMA: &str = "semaprax.image-function-instances.v1";
- IMAGE_INSTANCE_FACET_SCHEMA · constant · L6-L6 — pub const IMAGE_INSTANCE_FACET_SCHEMA: &str = "semaprax.image-instance-facet.v1";
- MAX_INSTANCE_BYTES · constant · L7-L7 — const MAX_INSTANCE_BYTES: usize = 65_536;
- MAX_DEPTH · constant · L8-L8 — const MAX_DEPTH: usize = 256;
- EVIDENCE · constant · L9-L9 — const EVIDENCE: &str = "descriptive_projection_of_retained_generic_instance_hir";
- NONCLAIMS · constant · L10-L16 — const NONCLAIMS: [&str; 5] = [
- Result · type · L17-L17 — type Result<T> = std::result::Result<T, Vec<Diagnostic>>;
- function_instances · function · L20-L59 — pub fn function_instances(
- expand_instance_facet · function · L62-L122 — pub fn expand_instance_facet(
- selector · function · L125-L136 — fn selector(value: &str, max: usize) -> Result<()>
- template · function · L137-L169 — fn template<'a>(
- inventory · function · L170-L220 — fn inventory<'a>(
- type_keys · function · L221-L243 — fn type_keys(types: &[hir::ResolvedType]) -> Result<Vec<String>>
- instance_handle · function · L244-L254 — fn instance_handle(
- cursor_for_instance · function · L255-L264 — fn cursor_for_instance(handle: &str, offset: usize, page_size: usize) -> String
- offset · function · L265-L281 — fn offset(handle: &str, cursor: Option<&str>, options: ImageFacetOptions) -> Result<usize>
- check_offset · function · L282-L290 — fn check_offset(offset: usize, total: usize, cursor: Option<&str>) -> Result<()>
- push · function · L291-L306 — fn push(items: &mut Vec<Value>, row: Value, bytes: &mut usize) -> Result<()>
- relationships · function · L308-L316 — fn relationships(image: &ProjectSemanticImage, instance: &ResolvedFunctionInstance) -> Vec<Value>
- callers · function · L318-L360 — fn callers(
- append_callers · function · L362-L431 — fn append_callers(

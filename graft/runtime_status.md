---
covers: []
---
# runtime_status.rs

- NEXT_ARENA_INSTANCE · constant · L15-L15 — static NEXT_ARENA_INSTANCE: AtomicU64 = AtomicU64::new(1);
- StatusToken · struct · L19-L19 — pub struct StatusToken(u32);
- SUCCESS · constant · L22-L22 — pub const SUCCESS: Self = Self(0);
- from_raw · function · L24-L26 — pub const fn from_raw(raw: u32) -> Self
- raw · function · L28-L30 — pub const fn raw(self) -> u32
- is_success · function · L32-L34 — pub const fn is_success(self) -> bool
- StatusContextId · struct · L42-L42 — pub struct StatusContextId(u64);
- new · function · L45-L47 — pub const fn new(nonce: u64) -> Self
- nonce · function · L49-L51 — pub const fn nonce(self) -> u64
- ScopedStatusToken · struct · L60-L64 — pub struct ScopedStatusToken
- context · function · L67-L69 — pub const fn context(self) -> StatusContextId
- token · function · L71-L73 — pub const fn token(self) -> StatusToken
- raw · function · L75-L77 — pub const fn raw(self) -> u32
- normalize_arithmetic · function · L81-L83 — pub fn normalize_arithmetic(case: StatusCase) -> NormalizedStatus
- normalize_contract · function · L89-L91 — pub fn normalize_contract(phase: ContractPhase) -> NormalizedStatus
- StatusArena · struct · L95-L100 — pub struct StatusArena
- new · function · L103-L116 — pub fn new(context: StatusContextId, capacity: u32) -> Result<Self, StatusArenaError>
- context · function · L118-L120 — pub const fn context(&self) -> StatusContextId
- capacity · function · L122-L124 — pub const fn capacity(&self) -> u32
- len · function · L126-L128 — pub fn len(&self) -> usize
- is_empty · function · L130-L132 — pub fn is_empty(&self) -> bool
- record · function · L134-L153 — pub fn record(
- record_arithmetic · function · L155-L160 — pub fn record_arithmetic(
- record_contract · function · L162-L167 — pub fn record_contract(
- resolve · function · L169-L185 — pub fn resolve(
- resolve_local · function · L188-L197 — pub fn resolve_local(&self, token: StatusToken) -> Result<&NormalizedStatus, StatusArenaError>
- StatusArenaError · enum · L201-L219 — pub enum StatusArenaError
- fmt · function · L222-L249 — fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result
- tests · module · L256-L256 — mod tests;

# Owned Data Browser v1

Hosted promotion fixture for the WP-10 direct-`Bytes` npm package. It consumes
only the exact generated six-file package and must exercise fresh byte results,
capacity edges, hostile typed arrays, repeated calls, failure settlement, and
artifact authentication in every browser named by the promotion workflow.

This fixture is not an activation or a current hosted-evidence claim.

schema = "semaprax.project.v8"
name = "owned-data-browser-variants"
version = "0.1.0"
profile = "owned-data-api.v1"
entry = "owned_browser_variants.app"
sources = ["src/app.spx", "src/tests.spx"]
web_exports = ["frame.maybe", "frame.result"]
tests = ["owned_browser_variants.tests"]

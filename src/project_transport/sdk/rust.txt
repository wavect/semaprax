// Generated SEMAPRAX Transport v6 codec. Caller owns all I/O and process plumbing.
use serde_json::{json,Map,Value};
pub const DISCOVERY_JSON:&str=__DISCOVERY__;
fn object<'a>(value:&'a Value)->Result<&'a Map<String,Value>,String>{value.as_object().ok_or_else(||"object required".into())}
fn keys(value:&Map<String,Value>,expected:&[&str])->Result<(),String>{if value.len()==expected.len()&&expected.iter().all(|key|value.contains_key(*key)){Ok(())}else{Err("closed object mismatch".into())}}
fn digest(value:&str)->bool{value.len()==71&&value.starts_with("sha256:")&&value.as_bytes()[7..].iter().all(|b|b.is_ascii_digit()||(b'a'..=b'f').contains(b))}
fn request(id:&Value,method:&str,params:Value)->Result<String,String>{if !(id.as_u64().is_some()||id.as_str().is_some_and(|v|!v.is_empty()&&v.len()<=128)){return Err("invalid id".into())}let mut line=serde_json::to_string(&json!({"jsonrpc":"2.0","id":id,"method":method,"params":params})).map_err(|e|e.to_string())?;if line.len()>1048576{return Err("request bound".into())}line.push('\n');Ok(line)}
pub fn describe(id:&Value,project_revision:&str,workspace_revision:&str)->Result<String,String>{if !digest(project_revision)||!digest(workspace_revision){return Err("subject revision".into())}request(id,"project/api-describe",json!({"project_revision":project_revision,"workspace_revision":workspace_revision}))}
pub fn build_inline(id:&Value,project_revision:&str,workspace_revision:&str,max_bytes:Option<u64>)->Result<String,String>{if !digest(project_revision)||!digest(workspace_revision){return Err("subject revision".into())}let mut params=json!({"project_revision":project_revision,"workspace_revision":workspace_revision});if let Some(limit)=max_bytes{if limit==0||limit>41943040{return Err("carrier bound".into())}params["max_bytes"]=json!(limit)}request(id,"project/npm-build-inline",params)}
pub fn decode(line:&str,id:&Value,with_build:bool)->Result<Value,String>{if line.len()>16777216{return Err("response bound".into())}let envelope:Value=serde_json::from_str(line).map_err(|e|e.to_string())?;let envelope=object(&envelope)?;keys(envelope,&["jsonrpc","id","result"])?;if envelope["jsonrpc"]!="2.0"||&envelope["id"]!=id{return Err("response binding".into())}let result=object(&envelope["result"])?;let expected=if with_build{&["project_schema","descriptor_schema","carrier_schema","descriptor","descriptor_digest","build"][..]}else{&["project_schema","descriptor_schema","carrier_schema","descriptor","descriptor_digest"][..]};keys(result,expected)?;let triples=[("semaprax.project.v8","semaprax.public-owned-data-api.v1","semaprax.project-npm-build.v7"),("semaprax.project.v9","semaprax.public-flat-owned-record-api.v1","semaprax.project-npm-build.v8"),("semaprax.project.v10","semaprax.public-owned-utf8-api.v1","semaprax.project-npm-build.v9"),("semaprax.project.v11","semaprax.public-nested-owned-record-api.v1","semaprax.project-npm-build.v10")];let triple=triples.iter().find(|row|result["project_schema"]==row.0).ok_or("profile binding")?;let descriptor=object(&result["descriptor"])?;let digest=result["descriptor_digest"].as_str().ok_or("digest")?;if result["descriptor_schema"]!=triple.1||result["carrier_schema"]!=triple.2||digest.len()!=71||!digest.starts_with("sha256:")||!digest.as_bytes()[7..].iter().all(|b|b.is_ascii_digit()||(b'a'..=b'f').contains(b))||descriptor.get("schema")!=Some(&Value::String(triple.1.into()))||descriptor.get("project_schema")!=Some(&Value::String(triple.0.into())){return Err("profile binding".into())}if with_build&&object(&result["build"])?.get("schema")!=Some(&Value::String(triple.2.into())){return Err("carrier binding".into())}Ok(Value::Object(result.clone()))}

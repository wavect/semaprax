schema = "semaprax.manifest.v1"

[package]
name = "std-format"
version = "0.1.0"
profile = "useful-data.v2"

[modules]
entry = "std.format.examples"
sources = ["src/examples.spx", "src/format.spx", "src/tests.spx"]
tests = ["std.format.tests"]

[exports]
web = []

[dependencies]
std.io = "=0.1.0"

schema = "semaprax.manifest.v1"

[package]
name = "std-path-normalize"
version = "0.1.0"
profile = "owned-data-api.v1"

[modules]
entry = "std.path.normalize.examples"
sources = ["src/examples.spx", "src/normalize.spx", "src/tests.spx"]
tests = ["std.path.normalize.tests"]

[exports]
web = []

[dependencies]
std.path.value = "=0.1.0"
